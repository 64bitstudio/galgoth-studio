package com.galgothstudio.backend.domain.export.bbmodel;

/** Referencia directa a un {@link BBElement} por su {@code uuid} -- igual que en v5. */
public record BBV4OutlinerLeaf(String uuid) implements BBV4OutlinerEntry {
}
