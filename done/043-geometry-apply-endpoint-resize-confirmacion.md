# 043 — POST /geometry/apply — confirmación de resize, commit en pointerup

**Milestone:** M7 · **Depende de:** 018, 020, 041 · **HUs:** HU-33 · **Épica:** L (`docs/definiciones/galgoth-studio-fase3-textura.md`)

## Objetivo
Nace de `docs/definiciones/galgoth-studio-fase3-textura.md` (Diseño técnico §2 y §15). Cierra el Hallazgo B heredado de Fase 1+2: el editor manual calcula geometría/UV 100% client-side sin que el backend lo revalide. Nuevo endpoint síncrono `POST /api/mobs/{mobId}/geometry/apply` (`MobGeometryController`, `project/api/` + `project/geometry/`, mismo patrón que `project/draft`), que ejecuta `GeometryEngine.apply`/`UvLayoutSelector` server-side — SOLO para `createCuboid`/`resizeCuboid`/`removeCuboid`. `moveCuboid`/`rotateCuboid`/pivot no afectan UV y siguen 100% client-side, sin cambios.

## Criterios de aceptación (TDD)
- Dado `POST /api/mobs/{mobId}/geometry/apply` con una operación `resizeCuboid` que no afecta ninguna cara `PAINTED`, cuando se invoca, entonces aplica la operación server-side (vía `GeometryEngine`/`UvLayoutSelector` de 041) y devuelve 200 con la geometría/UV resultante.
- Dado que la operación SÍ afecta una cara `PAINTED`, cuando se invoca sin `confirmPaintLoss`, entonces responde con el detalle de `PaintedRegionResizeConfirmationRequiredException` (4xx) sin aplicar nada.
- Dado el mismo caso reenviado con `confirmPaintLoss: true`, cuando se invoca, entonces aplica la operación (reempaqueta + `UvReservation`, mismo mecanismo de 041) y devuelve 200.
- Dado `createCuboid`/`removeCuboid`, cuando se envían a este endpoint, entonces siguen exactamente las reglas ya definidas en 041 (rechazo por `UvAtlasOverflowException` / marcado `ORPHAN`).
- Dado `moveCuboid`/`rotateCuboid`/una operación de pivot, cuando el usuario las ejecuta en el editor, entonces NUNCA disparan una llamada a este endpoint — siguen 100% client-side + autosave debounced (test de regresión: sin llamadas de red durante esas operaciones).
- **Latencia**: dado que el usuario arrastra el handle de resize de un cuboid (`pointermove`), cuando lo hace, entonces el frontend muestra un preview 100% local sin ninguna llamada de red — solo al `pointerup` se envía la ÚNICA llamada a `POST /geometry/apply` para esa operación (test de UI: cero requests de red durante N eventos `pointermove` simulados, exactamente 1 al `pointerup`).
- Dado que el backend responde con la excepción de confirmación tras el `pointerup`, cuando esto ocurre, entonces el frontend mantiene el preview visual del tamaño soltado (sin aplicar aún el efecto sobre UV) y muestra el modal de confirmación — "Confirmar" reenvía la MISMA operación con `confirmPaintLoss: true`; "Cancelar" no reenvía nada y el cuboid vuelve a su tamaño anterior.

## Hecho

**Backend** (`backend/src/main/java/com/galgothstudio/backend/`):
- `project/geometry/MobGeometryApplyService.java` (nuevo): whitelist cerrada (`Set.of(CreateCuboid.class, ResizeCuboid.class, RemoveCuboid.class)`) que rechaza cualquier otra operación con `UnsupportedGeometryApplyOperationException` ANTES de tocar el draft; lee el draft actual vía `DraftPersistenceService.getDraft` (mismos 404 `MOB_NOT_FOUND`/`DRAFT_NOT_FOUND` que `GET /draft`, reutilizados tal cual), aplica `GeometryEngine.apply(model, ops, uvLayoutStrategy, confirmPaintLoss)` y persiste el resultado con `DraftPersistenceService.autosave` (mismo `draft_version` que `PATCH /draft` — no se duplicó el mecanismo de persistencia).
- `project/geometry/MobGeometryApplyRequest`/`MobGeometryApplyResponse`/`UnsupportedGeometryApplyOperationException.java` (nuevos): DTOs y excepción de dominio del endpoint, mismo estilo que `project/draft`.
- `project/api/MobGeometryController.java` (nuevo): `POST /api/mobs/{mobId}/geometry/apply`, un solo método, delega 100% en el servicio.
- `ApiExceptionHandler.java`: 4 handlers nuevos — `UnsupportedGeometryApplyOperationException`→400 `UNSUPPORTED_GEOMETRY_OPERATION`; `GeometryValidationException`→400 `INVALID_GEOMETRY_OPERATION` (primera vez que esta excepción del ticket 005 cruza la frontera HTTP directamente); `UvAtlasOverflowException`→400 `UV_ATLAS_OVERFLOW` con el detalle de dimensiones (primera vez que cruza HTTP directamente); `PaintedRegionResizeConfirmationRequiredException`→**409** `PAINTED_REGION_RESIZE_CONFIRMATION_REQUIRED` con `details: ["<cuboidId>:<face>", ...]` (face en minúscula, mismo formato que el JSON de `FaceName`).
- **Wiring de `confirmPaintLoss` (el punto que 041 dejó explícitamente pendiente)**: `UvLayoutStrategy` gana una sobrecarga `default` de 5 argumentos (`layout(cuboids, w, h, previousLayout, confirmPaintLoss)`) que por default ignora el flag y delega en la de 4 argumentos — `AlphaAutoPackStrategy` no la sobreescribe, cero cambio de comportamiento. `StableUvStrategy` ya tenía este método exacto desde 041 (como método propio, no de la interfaz) — ahora lleva `@Override` real. `UvLayoutSelector` gana el override correspondiente, propagando el flag a la estrategia resuelta. `GeometryEngine.apply(model, ops, uvLayoutStrategy)` (3 args) ahora delega en un nuevo `apply(model, ops, uvLayoutStrategy, confirmPaintLoss)` (4 args) con `confirmPaintLoss=false` — mismo resultado exacto para los callers existentes (`GeometryPlannerService`, `AiGeometryEditPlannerService`), que nunca pasan por el caso de confirmación. `MobGeometryApplyService` es el único caller real que invoca la variante con `confirmPaintLoss` tomado del request.
- Tests nuevos: `MobGeometryControllerTest` (12 tests de integración Testcontainers+MockMvc — resize sin/con confirmación, confirmado, createCuboid/removeCuboid exitosos, createCuboid con overflow, whitelist rechazando moveCuboid/rotateCuboid/setBonePivot, mob/draft inexistentes) + tests de wiring en `UvLayoutSelectorTest`/`UvLayoutStrategyDefaultMethodTest`/`GeometryEngineUvSelectorIntegrationTest` (sobrecargas de 5/4 argumentos respectivamente). Backend: **312 tests, 0 failures** (+12 nuevos de este ticket, +300 heredados sin tocar).

**Frontend**:
- `frontend/src/editor/geometryApplyApi.ts` (nuevo): cliente HTTP de `POST /geometry/apply`; `PaintedRegionResizeConfirmationRequiredError extends ApiError` parsea `details` (`"cuboidId:face"`) a `AffectedFace[]` para el modal.
- `frontend/src/editor/geometryApplyStore.ts` (nuevo, Pinia store): orquesta Resize/Add/Remove server-side — `resizeCuboid`/`confirmPendingResize`/`cancelPendingResize` (estado `pendingResizeConfirmation` compartido) + `createCuboid`/`removeCuboid`. Un éxito o una confirmación aplican el resultado vía `draftModelStore.commitExternalModel` (el hook que 040 dejó preparado exactamente para esto) — nunca reimplementa la mutación a mano.
- `ThreeViewport.vue`: el modo `scale` del gizmo (`handleTransformMouseUp`) deja de llamar `draft.resizeSelectedCuboid` (local) y llama `geometryApply.resizeCuboid` (backend) — durante `objectChange` (equivalente a `pointermove`) no cambia nada (Three.js ya escala el mesh en vivo sin red, comportamiento preexistente reaprovechado como el preview). Si el backend exige confirmación, el mesh se deja tal cual (ninguna llamada a `setModel`); un `watch` sobre `pendingResizeConfirmation` resincroniza el mesh contra `draft.model` cuando el modal se resuelve (Confirmar deja el modelo ya actualizado, Cancelar lo deja intacto — un solo watcher cubre ambos casos). Move/Rotate sin cambios.
- `InspectorPanel.vue`: "Tamaño" (edición numérica, dispara en `change`, no en cada tecla) llama a `geometryApply.resizeCuboid` en vez de `draft.resizeSelectedCuboid`. Posición/Rotación/Pivot sin cambios.
- `EditorToolbar.vue`: "Agregar cuboide"/"Eliminar" pasan a `geometryApply.createCuboid`/`geometryApply.removeCuboid` (ambos ahora `async`); el id real de un cuboid creado lo asigna el backend (no se puede predecir client-side), se resuelve diffeando los ids de cuboids antes/después. "Duplicar" queda **deliberadamente sin tocar** (ver Hallazgo abajo).
- `MobEditor.vue`: monta UN `ConfirmDialog` (componente ya existente del design system, reutilizado tal cual) para el modal de confirmación de pérdida de pintura, compartido por los 3 puntos de entrada de Resize (gizmo 3D, input numérico) — el mensaje lista las caras afectadas por nombre de cuboid + cara.
- Tests nuevos: `geometryApplyApi.spec.ts` (parsing de la excepción de confirmación + errores genéricos), `geometryApplyStore.spec.ts` (resize exitoso/con confirmación/confirmar/cancelar, createCuboid con id real del backend, createCuboid rechazado, removeCuboid). Tests actualizados (comportamiento cambió deliberadamente, no un parche): `ThreeViewport.spec.ts` (scale ahora async vía backend + 2 tests nuevos de regresión de latencia — cero red en `objectChange`, exactamente 1 en `mouseUp` — + test de confirmación + regresión explícita "moveCuboid/rotateCuboid nunca llaman a `applyGeometry`"), `InspectorPanel.spec.ts` (Tamaño), `EditorToolbar.spec.ts` (Add/Delete, con mocks de `geometryApplyApi`). Frontend: **394 tests, 0 failures** (+13 nuevos, resto heredado). `vue-tsc -b`, `eslint . --max-warnings 0` y `vite build` en verde.

**Decisiones de diseño no triviales**:
1. `confirmPaintLoss` se expone end-to-end añadiendo UNA sola sobrecarga aditiva por capa (interfaz → `UvLayoutSelector` → `GeometryEngine`), replicando exactamente el patrón ya usado por la sobrecarga de 4 argumentos de 041 — cero cambio de algoritmo en `StableUvStrategy`/`BoxUvMath`.
2. El endpoint reutiliza `DraftPersistenceService.getDraft`/`autosave` tal cual en vez de reimplementar lectura/escritura de `mob_drafts` — significa que hereda el mismo modelo de concurrencia que `PATCH /draft` (última escritura gana, sin lock optimista contra `draft_version`); no es un caso nuevo, es el mismo ya aceptado para autosave.
3. El detalle de `PAINTED_REGION_RESIZE_CONFIRMATION_REQUIRED` se codifica como `details: ["cuboidId:face"]` (strings) en vez de agregar un campo nuevo a `ApiErrorResponse` — mantiene el contrato de error ya establecido (`{error, message, details: string[]}`) sin romperlo para ningún otro endpoint.
4. El modal de confirmación se centraliza en un store (`geometryApplyStore`) en vez de en cada componente que dispara un resize (gizmo 3D, input numérico) — un solo `ConfirmDialog` montado en `MobEditor.vue`, sin importar desde dónde se originó el resize.

**Hallazgo real, señalado explícitamente (no corregido en este ticket)**: "Duplicar cuboid" (`EditorToolbar.vue`) es, internamente, equivalente a un `createCuboid` — hereda el mismo Hallazgo B (UV client-side sin revalidar server-side) que este ticket cierra para "Agregar". El ticket 043 solo pide Resize/Add/Remove explícitamente; Duplicar no está en su alcance y se dejó exactamente como estaba. Queda como gap documentado para que el Product Owner decida si amerita un ticket propio.
