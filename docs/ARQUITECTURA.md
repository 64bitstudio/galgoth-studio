# Arquitectura — Galgoth Studio

Ver `docs/definiciones/galgoth-studio-mvp.md` (sección "Diseño técnico") para el diseño completo con tradeoffs — este archivo es el resumen pedagógico que se mantiene vivo conforme se implementa.

## Visión general

Monorepo con un frontend Vue 3 (editor 3D basado en Three.js) y un backend Spring Boot 4.1.0 / Java 25 (monolito modular, sin microservicios — corregido desde "3.x/Java 21" al implementar el ticket 003, ver Addendum de `docs/definiciones/galgoth-studio-mvp.md`), comunicados por REST + SSE. Postgres como base de datos (migraciones Flyway), MinIO como storage S3-compatible para imágenes/texturas/exports (levantado en el ticket 023, adelantado desde el 024 original — el pipeline de thumbnails necesitaba subir un asset ya, ver `docker/docker-compose.yml`).

```text
Frontend (Vue3+Three.js) --REST/SSE--> Backend (Spring Boot, monolito modular)
                                          ├─ project            (proyectos/mobs)
                                          ├─ asset              (imágenes de referencia, MinIO)
                                          ├─ ai-orchestrator     (jobs de IA, SSE)
                                          ├─ model-validation    (AutoUv, FmmCompatibilityValidator)
                                          └─ export              (BBModelExporter v4/v5)
                                        --> Postgres + MinIO
```

## Estado actual

- **Frontend**: sistema de diseño base implementado (`done/002-...`) — sin pantallas productivas todavía.
- **Backend**: esqueleto real Spring Boot funcionando (`done/003-...`) — `GalgothStudioApplication` arranca, se conecta a Postgres vía `docker/docker-compose.yml` (autodetectado por `spring-boot-docker-compose`), y Flyway aplica el esquema inicial (8 tablas, ver `docs/BASE_DE_DATOS.md`). Sin Dockerfile ni deploy real todavía — sin paquetes de dominio de negocio (`project`/`asset`/`ai-orchestrator`/`model-validation`/`export`) ni ArchUnit, que aterrizan con el primer código de negocio real (ticket 005+).
- **Dominio compartido** (`done/004-...`): `MobProjectModel` -- JSON Schema (`contracts/schemas/`, fuente de verdad formal), tipos TS (`frontend/src/domain/`) y DTOs Java (`backend/.../domain/model/`), verificados por round-trip contra el mismo fixture (`contracts/fixtures/model-spec-example.json`). `CoordinateSystemContract` (`docs/adr/0001-coordinate-system-contract.md`) fija unidades/ejes/rotación/composición padre-hijo, implementado en ambos lados (`coordinateSystem.ts` / `CoordinateSystem.java`) y verificado contra el mismo fixture (`contracts/fixtures/coordinate-system-fixture.json`) — todo módulo futuro que necesite convertir coordenadas usa exclusivamente estas funciones, no reimplementa la suya.
- **Geometry Engine** (`done/005-...`, `backend/.../domain/geometry/`): whitelist cerrada de 9 operaciones (`createBone`, `createCuboid`, `resizeCuboid`, `moveCuboid`, `rotateCuboid`, `setBonePivot`, `setBoneRotation`, `parentBone`, `removeCuboid` -- master prompt §9.2), deserializadas polimórficamente por el campo `"op"` (Jackson `@JsonTypeInfo`, un valor fuera de la whitelist hace fallar la deserialización del batch completo). `GeometryEngine.apply(model, operaciones)` es una función pura y atómica: si cualquier operación falla su validación, ninguna se aplica y el modelo de entrada nunca se toca. Resuelve referencias `tempRef` (forward-refs a ids generados en el mismo batch, p.ej. un `createCuboid` que referencia el `tempId` de un `createBone` anterior) siempre a UUIDs generados por el backend, nunca a ids provistos externamente. Es el único camino por el que cualquier escritura de geometría (manual server-side o IA, tickets 018/028/031) pasa -- todo el cálculo de rotación usa `CoordinateSystem` (ticket 004), nunca una reimplementación propia. Sin endpoint REST todavía (llega con el ticket que primero lo exponga).
- **AutoUv backend** (`done/006-...`, `backend/.../domain/uv/`): `UvLayoutStrategy` (interfaz) con una única implementación este ciclo, `AlphaAutoPackStrategy` -- desenvolvimiento de caja estándar de Minecraft (shelf-packing determinista, fórmula verificada contra el código fuente real de Blockbench, no inventada) que asigna UV válida a las 6 caras de cada cuboid dentro del atlas (`MobProjectModel.uv`). `GeometryEngine.apply(model, ops, uvLayoutStrategy)` la invoca automáticamente cuando el batch incluye `createCuboid`/`resizeCuboid` -- backend es la autoridad canónica, nunca confía en la UV que ya calculó el frontend localmente. Si el conjunto de cuboids no cabe en el atlas configurado, lanza `UvAtlasOverflowException` (`UV_ATLAS_OVERFLOW`) con las dimensiones mínimas que sí lo harían caber -- el atlas nunca crece en silencio.
- **AutoUv frontend** (`done/007-...`, `frontend/src/domain/autoUv.ts`): espejo TS exacto de `AlphaAutoPackStrategy` (misma fórmula de box-UV-unwrap, mismo shelf-packing, mismo `UvAtlasOverflowError`) -- función pura y síncrona, sin ninguna petición HTTP, para feedback inmediato en el editor manual (cada `Command` de crear/redimensionar un cuboid, tickets 015-020, la llamará directo). El backend sigue siendo la autoridad canónica. Paridad verificada con una fixture compartida (`contracts/fixtures/uv-layout-fixture.json`) ejecutada en AMBOS test suites (Vitest y JUnit) -- una divergencia entre TS y Java rompe el build de cualquiera de los dos lados, no solo uno.
- **Viewport Three.js** (`done/008-...`, `frontend/src/viewport/`): `buildMobScene.ts` construye una escena PLANA a partir de un `MobProjectModel` (un mesh por cuboid, sin anidar `Object3D` por bone -- `from`/`to`/`origin` viven en un espacio compartido tipo Minecraft/Blockbench, no en el espacio local del bone padre); para cada cuboid recorre su cadena de bones ancestros y compone las rotaciones con `applyPivotRotation`/`rotationMatrixFromEulerXYZDeg` del `CoordinateSystemContract`, generalizando `composeBoneChildTransform` a N niveles. `ThreeViewportService` es el renderer/canvas ÚNICO del proceso (patrón singleton, `attachTo`/`detach` mueven el mismo canvas entre contenedores) -- sienta la base para que 016 (viewport interactivo) y 023 (thumbnails) lo reutilicen sin instanciar un `WebGLRenderer` nuevo. Development harness en `/dev/viewport-harness` (no enlazado desde la navegación productiva) abre el sample real Carcomido -- convertido UNA VEZ a `MobProjectModel` por `contracts/fixtures/scripts/convert-carcomido-sample.py` (deliberadamente NO un `BBModelImporter` de producción, ver Addendum del doc de definición) y servido como fixture estática (`contracts/fixtures/carcomido-mob-project-model.json`, copiada a `frontend/public/dev-fixtures/` para que el harness la sirva sin parsear `.bbmodel` en runtime).
- **Viewport interactivo** (`done/016-...`): `ThreeViewportService` ahora incluye `OrbitControls` (rotar/pan/zoom con mouse, damping activo, `controls.update()` en cada frame del render loop) y un `GridHelper` de piso, ambos creados una única vez en el constructor del singleton. `resetCamera()` vuelve a la posición/ángulo inicial fijo. `buildMobGroup(model, selectedCuboidId?)` agrega un outline (`EdgesGeometry`+`LineSegments`, hijo del mesh -- hereda su transform automáticamente) al cuboid seleccionado; la sincronización real de selección con una jerarquía llega en 017, este parámetro es la superficie que ese ticket conectará a un store compartido. El dev harness (`/dev/viewport-harness`) gana el botón de reset de cámara (el selector de prueba del outline se reemplaza en 017 por el panel de jerarquía real).
- **Jerarquía + selección sincronizada** (`done/017-...`, `frontend/src/editor/`): primer store Pinia real del proyecto (`selectionStore.ts`, Pinia ya estaba registrado desde el bootstrap sin uso hasta ahora) -- fuente única de verdad de qué cuboid está seleccionado, compartida entre `HierarchyPanel.vue` (árbol recursivo bone→cuboids→sub-bones, construido por la función pura `buildHierarchyTree`) y `ThreeViewport.vue`. Sincronización bidireccional real: clic en un nodo del árbol llama `selection.select(id)` (el viewport reacciona vía `watch` y muestra el outline); clic en el canvas dispara un raycast (`ThreeViewportService.pickCuboidIdAt`, contra `userData.cuboidId` de cada mesh -- nunca el nombre, que puede repetirse) y actualiza el mismo store (el árbol reacciona y resalta el nodo). Un guard de distancia (`CLICK_DRAG_THRESHOLD_PX`) descarta como selección cualquier click cuyo `pointerdown` haya empezado a más de 5px -- evita que un arrastre de órbita (ticket 016) dispare una selección accidental al soltar. El dev harness ahora muestra jerarquía y viewport lado a lado, reemplazando el `<select>` de prueba del ticket 016.
- **`BBModelExporterV5`** (`done/010-...`, `backend/.../domain/export/`): exportador `.bbmodel` v5 (`meta.format_version: "5.0"`, separación `groups`/`outliner` real de Blockbench 5.0 -- verificada contra el código fuente real, no inventada). Copia directa de campos desde `MobProjectModel` (`origin`/`rotation` como `[x,y,z]`, sin transformación) per el mapeo ya fijado en el ADR 0001 -- este exportador no reinterpreta ejes/grados, solo aplica la conversión de 004. `groups[]` es un array PLANO (sin `children`); toda la jerarquía vive en `outliner` (árbol de `{uuid, isOpen, children}` para bones y uuids sueltos para cuboids hoja). El orden exacto de composición de rotación queda documentado como riesgo abierto de bajo impacto hasta que 012 lo valide contra fixtures reales de Blockbench.
- **Textura placeholder + atlas formal** (`done/011-...`): `BBModelExporterV5.export(model, uvLayoutStrategy)` (overload nuevo, el de 010 queda intacto) SIEMPRE recomputa la UV vía `AlphaAutoPackStrategy` antes de exportar -- backend es la autoridad canónica también en el punto de export, nunca confía en UV ya calculada. Genera y embebe una textura checkerboard (`PlaceholderTexture`, `java.awt.image.BufferedImage`+`ImageIO`, sin dependencia de display real) con dimensiones EXACTAS al atlas del modelo (`MobProjectModel.texture`), como data URI base64 en `textures[0]` -- el mismo índice que `Face.texture=0` ya asigna AutoUv (verificado contra `Face.getSaveCopy()` real de Blockbench: el índice referencia POSICIÓN en el array, no el campo `id`). Si el modelo no cabe en el atlas, `UvAtlasOverflowException` (006) se propaga tal cual -- el export falla explícito, nunca agranda la textura en silencio.
- **Fixtures de conformidad** (`done/012-...`): `contracts/fixtures/model-spec-example.json` expandido con casos borde (bone y cuboid con rotación no trivial, múltiples cuboids por bone, cuboid parentado directo al bone raíz), comparado contra un snapshot aprobado a mano (`backend/src/test/resources/fixtures/bbmodel-golden/`). `carcomido_minecraft_cuboids.bbmodel` (real, del build pack) se usa como fixture de conformidad adicional vía un parser TEST-ONLY (`backend/.../export/blockbenchreal/`, nunca importado por código de producción) que prueba, de forma independiente del script Python del ticket 008, que el dominio Java puede representarlo sin pérdida. **5 fixtures reales adicionales** (`blockbench-real/v5/`, cuboid simple/jerarquía/pivots/multi-cuboid/UV-textura), provistas por el PO desde una instalación real de Blockbench -- un primer paquete resultó generado programáticamente (no real, admitido por su propio README) y se pidió repetir el paso real antes de aceptarlo; los archivos finales tienen campos idiosincrásicos reales de Blockbench (`unhandled_root_fields`, `mirror_uv`, etc.) que confirman la procedencia. **Hallazgo real**: el fixture `01_cuboid_simple` cuelga el cuboid directo de la raíz del `outliner` sin ningún bone -- Blockbench lo permite, nuestro dominio exige `Cuboid.boneId` no-nulo (animación); el parser test-only sintetiza un bone raíz implícito para representarlo sin pérdida de geometría, documentado explícitamente.
- **`FmmCompatibilityValidator`** (`done/013-...`, `backend/.../domain/export/validation/`): valida un `.bbmodel` ya exportado (JSON válido, UUIDs únicos entre `elements`/`groups`, referencias de `outliner` resueltas, dimensiones de cuboid > 0, UV de cada cara dentro del atlas, índices de textura existentes) y reporta hallazgos específicos (`ValidationIssue{severity, rule, element, message}` -- nunca un mensaje genérico). `ValidationResult.pass()` es explícito: `true` solo si no hay ningún `ERROR` (los `WARNING` se reportan igual, no bloquean). **Corrección real encontrada al implementarlo**: la convención de nombres especiales de bone de FreeMinecraftModels que `TECHNICAL_REFERENCES.md` resumía como `hitbox`/`tag_name`/`mount_*` es imprecisa -- el código fuente real (`BoneBlueprint.java`) usa prefijos `tag_`/`b_`/`h_`/`m_` (NO `mount_`) más los nombres EXACTOS `hitbox`/`tag_name`, y estos dos últimos tienen una consecuencia real no documentada: `BoneBlueprint.generateAndWriteCubes` nunca escribe la geometría de sus cuboids hijos -- el validador lo reporta como `WARNING` (`SPECIAL_BONE_GEOMETRY_DROPPED`). `galgoth_studio_build_pack/` no se tocó (assets fuente); la corrección queda documentada aquí y en el propio ticket.
- **`BBModelExporterV4`** (`done/014-...`): exporta la MISMA geometría/jerarquía que `BBModelExporterV5` (comparten helpers vía `BBModelExportSupport`, extraído de V5 al agregar V4 para no duplicar lógica), pero sin la separación `groups`/`outliner` de Blockbench 5.0 -- cada group (bone) embebe sus datos completos directo dentro del árbol `outliner` (`BBV4OutlinerGroup`), como escribía Blockbench 4.x. `format_version: "4.10"` y la ausencia de la clave `groups` se verificaron contra el `.bbmodel` real del proyecto, igual criterio que v5. No bloquea el Gate M1 (solo exige v5); requisito del Gate M6, cumplido con las 5 fixtures v4 reales (`blockbench-real/v4/`, "Export Legacy Project" desde el mismo proyecto de Blockbench de 012) validadas por el mismo test parametrizado que cierra 012.
- **Gate M1 de la épica 009, verificado con Blockbench real por primera vez** (2026-09-09): tomando el mismo `.bbmodel` real de 14 cuboides ya usado en las verificaciones en vivo de 030/032 (exportado por nuestro propio `BBModelExporterV5`), el PO lo abrió en su instalación real de Blockbench -- abrió limpio, sin ningún diálogo de reparación. Épica 009 cerrada (`done/009-...`).
- **Herramientas de transformación + Add/Delete/Duplicate** (`done/018-...`, `frontend/src/editor/`): `geometryOperations.ts` -- espejo TS puro del `GeometryEngine` backend (005), aplicado función por función (no batch/`tempRef`, al ser UI de confianza en vez de entrada de IA) -- y `draftModelStore.ts` (Pinia), el estado en memoria del mob editado que `ThreeViewport.vue`/`HierarchyPanel.vue` leen directamente (ya no reciben el modelo por prop). Gizmos 3D de arrastre (`TransformControls` de Three.js) para Move/Scale/Rotate sobre el patrón "commit al soltar" -- la mutación real (y su validación) solo ocurre en `mouseUp`, nunca por frame, porque `setModel()` reconstruye todos los meshes en cada cambio del modelo y rompería la referencia que el gizmo arrastra a mitad de un drag. Ver detalle completo (incluida la limitación del anillo de rotación libre y el hueco de `@types/three`) en `docs/COMPONENTES.md`. Verificado en vivo contra el fixture real Carcomido -- incluye un hallazgo real (no bug): su atlas UV de 64×64 ya está saturado, así que Add cuboid/Duplicate son rechazados en vivo por `UvAtlasOverflowError` (007), confirmando la integración extremo a extremo con AutoUv.
- **Command stack de Undo/Redo** (`done/019-...`, `frontend/src/editor/draftModelStore.ts`): un "Command" es simplemente la referencia al `MobProjectModel` inmediatamente anterior a cada edición exitosa, en una pila -- seguro sin clonar nada porque `geometryOperations.ts` (018) es puro (nunca muta su modelo de entrada). `undo()`/`redo()` solo reasignan `model`, nunca tocan el concepto de `mob_revision` (que ni existe todavía en este store, ver 020). Una operación rechazada nunca genera un Command; `load()` reinicia ambas pilas; un Command nuevo tras un `undo()` descarta la rama de redo pendiente (historial lineal). `EditorToolbar.vue` expone botones Undo/Redo + atajos Cmd/Ctrl+Z / Cmd/Ctrl+Shift+Z (ignorados con el foco en un input/textarea).
- **Draft persistence + autosave + Guardar** (`done/020-...`, `backend/.../project/`): primeros endpoints REST reales del proyecto (`docs/API.md`). `DraftPersistenceService` implementa el ciclo Command (cliente) → Draft (`mob_drafts`, autosave) → Revision (`mob_revisions`, inmutable) de HU-08/HU-09/HU-22. El autosave (`PATCH /draft`) persiste SOLO en cambio material -- dirty-check por igualdad estructural de `MobProjectModel` (records con equals de valor correcto desde el ticket 004, sin necesidad de comparar JSON como texto) -- y NUNCA valida contra el JSON Schema ni crea una revisión. `Guardar` (`POST /revisions`) es la única acción que sí valida (`MobProjectModelValidator`, nuevo paquete `modelvalidation`) y crea una fila inmutable, comparando contra la última revisión para no duplicar si no hubo cambios; deliberadamente NO toca `mob_drafts` (son dos propósitos independientes -- el siguiente autosave los realinea solo). VoBo explícito del Product Owner: este ticket es backend-only -- el harness de desarrollo (008) sigue con su fixture estático; el Gate M2 de la épica 015 queda demostrado a nivel de API (Testcontainers + MockMvc, mismo patrón que `SchemaConstraintsTest` del 003), y se conecta a una pantalla real cuando el ticket 021 (CRUD de mobs) exista.
  - **Hallazgo arquitectónico real, con impacto más allá de este ticket**: Spring Boot 4.1.0 cambió su `ObjectMapper` autoconfigurado por defecto de Jackson 2 (`com.fasterxml.jackson.*`) a Jackson 3 (`tools.jackson.*`, paquete nuevo) -- los módulos `Vec3JacksonModule`/`Vec4JacksonModule` (ticket 004, que serializan `Vec3`/`Vec4` como array `[x,y,z]` en vez del objeto `{x,y,z}` por defecto) están escritos contra la API clásica de Jackson 2 y NO se aplican al ObjectMapper Jackson 3 que Spring MVC usa por defecto para los bodies HTTP. Esto nunca se notó antes porque ningún controlador REST existía hasta este ticket (los tests siempre construían su propio `ObjectMapper` a mano). Se resolvió (`JacksonConfig`) definiendo explícitamente un `ObjectMapper` Jackson 2 (`@Primary`, con los módulos + `JavaTimeModule`) y un `MappingJackson2HttpMessageConverter` construido a partir de él -- pero esa clase está marcada *deprecated for removal* en Spring Framework 7, así que es un parche funcional, no la solución final. **Pendiente, fuera de alcance de este ticket**: evaluar una migración completa del backend a Jackson 3 (reescribir los módulos Vec3/Vec4 con la API nueva) en un ticket dedicado antes de que Spring elimine el soporte clásico.
  - `contracts/schemas/*.json` se copia al classpath del backend en build time (`processResources` en `build.gradle`) -- antes de este ticket, `MobProjectModelValidator` (y su predecesor de test, `MobProjectModelSchemaTest`) solo se podían leer con una ruta relativa `../contracts/schemas/...` que jamás habría funcionado desde un JAR desplegado.
- **CRUD de proyectos + dashboard** (`done/021-...`): primera pantalla productiva real del frontend (`frontend/src/projects/`, reemplaza `HomePlaceholder.vue`) + `backend/.../project/{ProjectController,ProjectService}` + `ProjectEntity`. Primera integración HTTP real frontend↔backend (CORS habilitado en el backend para el origen de Vite -- `docs/definiciones/galgoth-studio-mvp.md` §9 confirma CORS como la estrategia elegida, no un proxy de Vite). "Delete" es soft-delete (`projects.deleted_at`, ya en el esquema desde el ticket 003). "Duplicate" es una copia PROFUNDA (VoBo explícito del PO): proyecto + todos sus mobs + su historial COMPLETO de `mob_revisions` + su `mob_drafts` actual. "Export" del menú de acciones (mockup 01) se muestra deshabilitado con su razón visible -- no existe ningún endpoint de exportación de proyecto (decisión del PO); mismo criterio para la tarjeta CTA "Crear un mob con IA" (sin pipeline de IA todavía). El detalle de proyecto (`ProjectDetailPlaceholder.vue`) es deliberadamente mínimo -- el grid completo de mobs es HU-04/ticket 022.
  - **2 hallazgos reales, ambos encontrados en la verificación en vivo (no en tests) y corregidos en el mismo ticket**: (1) el `ObjectMapper` de `JacksonConfig` (construido a mano desde el ticket 020, ver esa nota arriba) tenía `WRITE_DATES_AS_TIMESTAMPS` habilitado (el default "puro" de Jackson, que Spring Boot normalmente desactiva vía su propia autoconfiguración -- perdida al construir el ObjectMapper manualmente) -- todo `Instant` (incluidos los de `AutosaveResponse`/`DraftView` del ticket 020) serializaba como epoch-seconds fraccionario (ej. `1788924939.428339`), no como texto ISO-8601. Corregido con `.disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS)` -- arregla también, retroactivamente, los endpoints del ticket 020. (2) `GSidebar`'s `@select` estaba cableado como no-op en ambas pantallas nuevas pese a que el comentario de cabecera ya afirmaba que "Inicio"/"Mis proyectos" navegaban -- clic en el sidebar desde el detalle de un proyecto no hacía nada. Corregido cableando ambas claves a `router.push('/projects')`.
- **CRUD de mobs + detalle de proyecto + "Agregar mob"** (`done/022-...`): `backend/.../project/mob/{MobService,...}` + `MobController` -- crear y listar mobs de un proyecto (Rename/Delete/Duplicate a nivel de mob individual no están en el alcance de este ticket). `frontend/src/projects/ProjectDetail.vue` (mockup 12) reemplaza el placeholder mínimo del 021: header + buscador (filtro client-side) + grid de `MobCard` (miniatura + `GStatusPill`, mapeando `in_progress` de la BD a `in-progress` del componente) + tarjeta CTA "+ Nuevo mob". `AddMobModal.vue` (mismo patrón de `<dialog>` nativo del 021) preserva la idea útil de `add_mob_modal_legacy.png` (una selección categorizada antes de confirmar) adaptada al dominio real de Galgoth: la "categoría" es el campo `baseType` (los 5 valores del `CHECK` de `mobs.base_type`), no un catálogo de mobs vanilla de Minecraft (ese producto legacy permite elegir un mob específico existente para re-texturizar, algo ajeno al modelo de Galgoth). Un mob nuevo siempre arranca en `status="draft"`, `current_revision_number=0`, sin fila en `mob_drafts` -- verificado en vivo contra Postgres real, no solo en tests.
- **Pipeline de thumbnails client-side** (`done/023-...`): primer paquete `asset` real del backend -- `AssetStorageService` (cliente S3 genérico, AWS SDK v2 contra MinIO vía `endpointOverride`+`forcePathStyle(true)`, ya que MinIO no soporta direccionamiento virtual-hosted-style) + `MinioConfig`/`AssetStorageProperties`. `ThumbnailService` (paquete `project.thumbnail`) es su primer consumidor: key interna fija `mobs/{mobId}/thumbnail.png` (siempre sobreescrita in-place -- un upload fallido nunca corrompe el thumbnail anterior, gratis por ser un PUT S3 atómico), pero `mobs.thumbnail_key` en la BD guarda el **path servible relativo** (`/api/mobs/{mobId}/thumbnail`), no la key interna -- el frontend antepone su propio `API_BASE_URL` sin conocer el detalle de almacenamiento (`thumbnailUrl()` en `frontend/src/api/apiConfig.ts`, usado por `ProjectCard.vue`/`MobCard.vue`). `ThreeViewportService.captureThumbnail()` reutiliza el mismo renderer/escena singleton (008) -- mueve la cámara al ángulo isométrico fijo (`DEFAULT_CAMERA_POSITION`/`TARGET`, ya existentes desde 016), renderiza un frame, captura vía `canvas.toBlob()`, y SIEMPRE restaura la cámara del usuario (`try/finally`) para que su vista no salte, incluso si la captura falla. El botón "Guardar" real de `EditorToolbar.vue` (añadido en este mismo ticket -- el 020 solo implementó el backend) sigue el patrón "commit primero, thumbnail después": `saveRevision()` se ejecuta primero; SOLO si tiene éxito se intenta `captureThumbnail()`+`uploadThumbnail()` en un try/catch independiente que únicamente hace `console.warn` si falla -- el thumbnail es un asset derivado best-effort, su fallo nunca revierte ni bloquea el Guardar ya completado (verificado en vivo: un `POST /thumbnail` con error transitorio no afecta el mensaje "Guardado" ya mostrado).
  - **Hallazgo real (naming de Testcontainers 2.x)**: el artefacto correcto para MinIO es `org.testcontainers:testcontainers-minio:2.0.5` (NO `org.testcontainers:minio`, que no resuelve) -- Testcontainers 2.x renombró sus módulos con prefijo `testcontainers-` (igual que `testcontainers-postgresql`); al no estar gestionado por el BOM de Spring Boot, la versión debe fijarse a mano. Sin soporte `@ServiceConnection` para MinIO -- se usa el patrón `@DynamicPropertySource` + contenedor `static final` con arranque EAGER (`static { MINIO_CONTAINER.start(); }` en `TestcontainersConfiguration`, antes de que corran los métodos `@DynamicPropertySource`, que no pueden leer un campo `@Autowired` porque se ejecutan antes de refrescar el contexto).
  - `ViewportHarness.vue` (dev harness, 008) ahora busca-o-crea un proyecto/mob real ("Dev Harness"/"carcomido-harness", vía 021/022) contra el backend al montar, y sobreescribe `mobId`/`projectId` del fixture estático de Carcomido con los ids reales -- necesario porque Guardar/thumbnail requieren un mob que exista de verdad, conservando toda la geometría rica del fixture intacta.
- **Asset-service: subida de imagen de referencia** (`done/024-...`, HU-10): `project/reference/ReferenceImageService` + `MobReferenceImageController` -- primer consumidor de `AssetStorageService` (023) además de los thumbnails, confirmando que quedó genérico/reutilizable como estaba planeado. A diferencia del thumbnail (una key fija por mob, sobrescrita), `reference_images` es append-only (puede haber varias imágenes por mob) -- cada subida genera una key única (`mobs/{mobId}/references/{id}.<ext>`). Límites de formato/tamaño (solo PNG/JPEG, máx 10MB) resueltos aquí con VoBo explícito del PO, tal como el documento de definición los dejaba abiertos a este ticket. **Backend-only, deliberado** -- la UI real del paso "Referencia" del wizard (mockups 02-04) llega en el ticket 027, que depende explícitamente de este (mismo patrón ya usado en 020→021).
  - **Hallazgo real, corregido en este mismo ticket**: comparar el header `Content-Type` recibido por igualdad de string exacta contra la whitelist (`"image/png"`) rechaza imágenes VÁLIDAS -- confirmado real con el propio `MockHttpServletRequestBuilder` de Spring Test, que agrega `;charset=UTF-8` a cualquier content-type, no solo texto. Corregido normalizando a `tipo/subtipo` (`MediaType.parseMediaType(...).getType()+"/"+getSubtype()`) antes de comparar, descartando cualquier parámetro adicional.
- **Interfaces de proveedores IA + `ClaudeProvider` + `MockProvider`** (`done/025-...`, HU-13, master prompt §20): nuevo paquete `aiorchestrator.provider`. Tres interfaces estables (`VisionModelProvider`/`StructuredReasoningProvider`/`ImageGenerationProvider`) devuelven el JSON crudo del modelo (`AiProviderResponse`), SIN VALIDAR -- esa responsabilidad es de los tickets 028/031, que consumirán estas interfaces. `ClaudeVisionProvider`/`ClaudeReasoningProvider` llaman la API real de mensajes de Anthropic (`POST /v1/messages`, sin SDK oficial de Java -- se llama directo con `RestClient`, ver hallazgo abajo) delegando la mecánica HTTP compartida a `ClaudeMessagesClient`; `MockVisionProvider`/`MockReasoningProvider`/`MockImageProvider` son dobles deterministas configurables (uso exclusivo en tests/desarrollo), con ejemplos por defecto tomados literalmente del master prompt §9.1/§9.3. Selección por variable de entorno `AI_VISION_PROVIDER`/`AI_REASONING_PROVIDER` (`AiProviderConfig`, `@ConditionalOnProperty`, default `claude`) -- ningún código de `ai-orchestrator` ni del dominio depende de cuál esté activo. `ImageGenerationProvider` queda definida pero SIN proveedor real este ciclo (Claude no genera imágenes; la textura pintada que la consumiría es Fase 3, fuera de alcance del Technical Alpha) -- solo `MockImageProvider` la implementa, dejando la abstracción completa para cuando el consumidor real llegue.
  - **Hallazgo real de wiring, con lección reutilizable**: un primer diseño (`ClaudeProvider implements VisionModelProvider, StructuredReasoningProvider` en una sola clase, expuesta bajo cada interfaz vía `@Bean` condicional) rompía la selección por entorno -- Spring resuelve `getBean(Interfaz.class)`/`@Autowired` inspeccionando el TIPO REAL de un singleton ya instanciado, no solo el tipo declarado del método `@Bean` que lo creó, así que la MISMA instancia seguía siendo candidata de ambas interfaces sin importar cuál bean la expuso (confirmado real: con ambas propiedades en "mock", `getBean(VisionModelProvider.class)` encontraba también el bean expuesto como `StructuredReasoningProvider`). Ni `@Primary` en las 4 fábricas lo resolvía (dos `@Primary` genuinos compitiendo entre sí). Solución real: una clase por interfaz (`ClaudeVisionProvider`/`ClaudeReasoningProvider`, `MockVisionProvider`/`MockReasoningProvider`), ninguna implementando más de una interfaz de proveedor -- la mecánica HTTP compartida de Claude vive en `ClaudeMessagesClient`, que no implementa ninguna interfaz de proveedor y por eso es segura de compartir como bean único.
  - **Hallazgo real de dependencias**: Spring Boot 4.x separó el soporte de cliente HTTP (`RestClient`/`RestTemplate` + su autoconfiguración, incluido el bean `RestClient.Builder`) del starter de servidor MVC -- `spring-boot-starter-webmvc` (ya presente desde el ticket 003) NO lo trae. Sin `spring-boot-starter-restclient` agregado explícitamente, `RestClient.Builder` simplemente no existe como bean y CUALQUIER `@SpringBootTest` (no solo los de IA) falla al arrancar el contexto completo -- mismo patrón de "un `@PostConstruct`/bean nuevo en el paquete raíz rompe toda la suite" ya visto en el ticket 023 con MinIO, pero esta vez la causa es una dependencia de Gradle faltante, no una property de test mal ubicada.
  - **Hallazgo real de serialización**: pasar un `byte[]` pre-serializado a mano (`objectMapper.writeValueAsBytes(body)`) como body de un `RestClient` request, con `Content-Type: application/json`, es ambiguo para un `HttpMessageConverter` de Jackson -- si ese converter tiene prioridad sobre el `ByteArrayHttpMessageConverter` por defecto (como en un test que registra el converter Jackson 2 explícito en la posición 0), Jackson serializa el `byte[]` como STRING BASE64 (su comportamiento por defecto para arrays de bytes), no como los bytes crudos esperados -- confirmado real capturando el body exacto enviado. Corregido pasando el `ObjectNode` directamente a `.body(...)` y dejando que el converter lo serialice, eliminando la ambigüedad de raíz.
  - `ClaudeMessagesClientTest`/`ClaudeVisionProviderTest`/`ClaudeReasoningProviderTest` verifican la forma REAL del request/response contra un `MockRestServiceServer` (nunca la API real, cero costo en CI). Un `ClaudeMessagesClientWiringTest` adicional (permanente, con Testcontainers) prueba con el `RestClient.Builder` REAL inyectado por Spring (no uno construido a mano) que la serialización funciona con la configuración real de la app -- necesario porque la verificación manual en vivo (ver abajo) encontró que un `RestClient.builder()` "a pelo" en un test SÍ tiene este problema, y había que confirmar que la app real no lo comparte.
  - **Verificación en vivo real completada** contra la API real de Anthropic (key scoped a un workspace, provista por el Product Owner): `ClaudeVisionProvider` analizó una imagen real y devolvió una descripción coherente y correcta, confirmando modelo (`claude-sonnet-5`), autenticación, formato de request/response y parseo end-to-end. Hallazgo real de cuenta encontrado en el camino: una API key a nivel de ORGANIZACIÓN (no scoped a un workspace específico) es rechazada por la API con 400 (`anthropic-workspace-id` requerido) -- resuelto generando una key scoped a un workspace, sin cambios de código.
- **Wizard 4 pasos UI** (`done/027-...`, HU-10, épica 026 -- abre M4's Wizard de generación IA): `frontend/src/ai/` -- ver `docs/COMPONENTES.md` para el detalle completo de los 4 pasos. **Decisión de alcance, VoBo explícito del PO**: aunque el ticket solo depende formalmente de 024/002 (ambos done), su propio AC menciona comportamiento de 028 (tipo base propuesto por IA) y 029 (disparar el job) -- ninguno existe todavía (subtareas posteriores de la misma épica). Se construyó la navegación completa + pasos 1-2 (Referencia/Configuración) totalmente reales (crean un mob real vía 022 y suben su referencia real vía 024), y los pasos 3-4 (Generación/Resultado) como shells visuales fieles a los mockups 03/04 pero sin job real -- 029/030 los conectan. Épica 026 y este ticket movidos a `in-process`/`done` juntos (mismo patrón que epic 015→016).
- **Vision→ModelIntent + Geometry planner** (`done/028-...`, HU-13/HU-14/HU-15, master prompt §9.1/§9.2): pipeline de generación completo (029 lo volvió async con SSE, ver bullet siguiente). Nuevo `contracts/schemas/model-intent.schema.json` (contrato de `ModelIntent`, ticket 004-style) + `ModelIntentValidator` (mismo patrón que `MobProjectModelValidator`, 020). Dos etapas, dos llamadas a IA independientes:
  1. `aiorchestrator.vision.VisionAnalysisService`: imagen+baseType → `VisionModelProvider` (025) → JSON crudo validado contra el schema de `ModelIntent` ANTES de deserializar (AC #1 -- una respuesta inválida nunca genera geometría).
  2. `aiorchestrator.planner.GeometryPlannerService`: `ModelIntent` → `StructuredReasoningProvider` (025) → `GeometryOperation[]` (deserialización polimórfica de 005, la whitelist se aplica gratis) → `GeometryEngine.apply(model, ops, uvLayoutStrategy)` (005/006) sobre un `MobProjectModel` vacío -- la propuesta final YA es un `MobProjectModel` completo con su UV recalculada, no una lista de operaciones cruda (para que 030 la consuma directo).

  `aiorchestrator.MobGenerationService` orquesta ambas etapas + persiste `ai_jobs` (AC #4: provider/model/promptVersion/schemaVersion/referenceIds/proposal_jsonb).
  - **Hallazgo real de transacciones (028, ya no aplica tal cual desde 029)**: la primera versión (síncrona, una sola transacción larga) capturaba una excepción de validación, persistía la fila `failed`, y RE-LANZABA la excepción -- sin `@Transactional(noRollbackFor=...)` explícito, Spring marcaba la transacción completa como rollback-only por la excepción saliente, deshaciendo la propia fila `failed` recién guardada. 029 eliminó el problema de raíz al partir cada escritura en su propia transacción corta (ver bullet siguiente) -- ya no hay una transacción larga que envolver.
  - **Tres hallazgos reales de la API de Anthropic, encontrados en la verificación en vivo contra `carcomido_reference.png` (nunca en tests automatizados, que usan exclusivamente `MockProvider`)**: (1) Claude envuelve la respuesta en un bloque de código Markdown (```` ```json ... ``` ````) con cierta frecuencia pese a que el prompt pide explícitamente "sin texto antes ni después" -- `ClaudeMessagesClient` ahora despoja el fence de forma defensiva, en el único punto por el que pasan AMBAS llamadas (visión y razonamiento). (2) `claude-sonnet-5` emite un bloque `"type":"thinking"` (razonamiento extendido) ANTES del bloque de texto real, sin que el request lo pida explícitamente -- con `max_tokens=4096` el modelo agotó el presupuesto completo pensando y nunca llegó a emitir texto (`stop_reason:"max_tokens"`); subido a 16000. (3) Un atlas de textura inicial de 64x64 (default histórico de fixtures de test) no alcanza para un rig humanoide real generado por IA (`UvAtlasOverflowException` pidió 64x70 mínimo) -- el modelo vacío de arranque de la generación ahora usa 128x128 (mismo default "recomendado" del wizard, 027).
  - **Verificación en vivo real completada, confirma HU-15 cualitativamente**: contra `carcomido_reference.png` real, la propuesta generada tiene 6 bones (body/head/right_arm/left_arm/right_leg/left_leg -- rig humanoide completo, neutral, animable) y 11 cuboids, incluyendo `right_hand_claws`/`left_hand_claws` (manos claramente sobrescaladas respecto al brazo), `torso_rags`/`right_sleeve_rag`/`left_sleeve_rag` (silueta de ropa dañada) y dimensiones de brazo izquierdo/derecho ligeramente distintas (asimetría real, no simulada) -- las 3 características cualitativas que pide el AC #3, producidas por la API real, no un fixture.
- **SSE de progreso con preview** (`done/029-...`, HU-11, master prompt §5): vuelve asíncrono el pipeline de 028. `POST /api/mobs/{mobId}/generate` (crea `ai_jobs` en `running` de inmediato, `provider`/`model` en placeholder `"pending"` hasta la primera respuesta real -- cierra el hueco documentado en 028 de un fallo de red sin fila persistida) dispara `MobGenerationService.runPipeline` en `generationExecutor` (`ThreadPoolTaskExecutor` real, `GenerationExecutorConfig`), fuera de cualquier transacción larga -- cada escritura a `ai_jobs`/`ai_job_events` es su propia transacción corta vía Spring Data. `GET /api/jobs/{jobId}/events` (SSE, `GenerationEventBroadcaster` en memoria + backlog persistido en `ai_job_events`) reproduce el historial vía `Last-Event-ID` antes de continuar en vivo -- suscribe ANTES de leer el backlog (duplicado posible en una ventana angosta, nunca pérdida; el cliente deduplica por `seq`). `POST /api/jobs/{jobId}/cancel` señala un `GenerationCancellationRegistry` en memoria, revisado en puntos de control entre etapas (nunca interrumpe una llamada HTTP a IA ya en vuelo -- cancelación honesta, no instantánea).
  - **Preview no persistente (AC #2)**: `GenerationPreviewDiff` (función pura) compara el modelo antes/después de aplicar cada operación (`GeometryEngine.apply`, sin UV, barato) y emite solo lo que cambió (`preview_operations`: bones/cuboids agregados/actualizados + ids removidos, ya resueltos -- nunca las `GeometryOperation` crudas con `tempId`). Genérico sobre las 9 operaciones de la whitelist (005) -- nunca diverge del motor real. El único `preview_snapshot` se emite en el evento `completado`, como resincronización natural. El modelo aplicado incrementalmente vive SOLO en el log de eventos (`ai_job_events`) y en memoria del frontend (`GenerationPreviewViewport.vue`, que envuelve `ThreeViewportService` DIRECTO, sin pasar por `useDraftModelStore` -- el store real del editor manual, 018) -- nunca toca `mob_drafts`/`mob_revisions`.
  - **Verificación en vivo real completada, end-to-end contra la API real de Anthropic**: click "Generar con IA" en el wizard real → las 4 etapas avanzaron en vivo con mensajes reales (`"Silueta detectada: Zombi humanoide encorvado, ropas desgarradas colgando, con grietas de energía púrpura recorriendo el cuerpo"`) → el rig se renderizó incrementalmente en el viewport 3D a medida que llegaban los 28 eventos `preview_operations` reales → `ai_jobs.status='completed'`, `provider='claude'`, `model='claude-sonnet-5'`, 29 eventos en `ai_job_events` con `seq` estrictamente secuencial terminando en `completado`/100/`preview_snapshot`. Cancelación verificada por separado: click "Cancelar" mid-flight (durante la llamada real al `StructuredReasoningProvider`) → confirmación explícita → el job terminó `status='cancelled'`, `proposal_jsonb IS NULL`, `finished_at` seteado -- ningún draft/revisión tocado.
- **Pantalla Resultado: Descartar/Regenerar/Usar este modelo** (`done/030-...`, HU-12): `GenerationResultService` (nuevo, mismo paquete `aiorchestrator`) es el lado "leer/aceptar" del pipeline -- nunca re-ejecuta `GeometryEngine` ni llama a ningún proveedor de IA. `GET /api/jobs/{jobId}/result` deserializa `ai_jobs.proposal_jsonb`, cuenta bones/cuboids, y calcula el estado REAL de compatibilidad FMM exportando la propuesta a `.bbmodel` (`BBModelExporterV5`, 010/011) y corriéndole `FmmCompatibilityValidator` (013) encima -- nunca un estimado. `POST /api/jobs/{jobId}/apply` ("Usar este modelo") delega en `DraftPersistenceService.applyGenerationProposal` (nuevo método, mismo servicio de "Guardar", 020): crea `mob_revisions` (`created_by='ai'`, valor ya contemplado por el `CHECK` de 003) Y `mob_drafts` en la MISMA transacción, avanzando `mobs.current_revision_number` -- a diferencia de `saveRevision` (020), que deliberadamente NUNCA toca `mob_drafts`. "Descartar"/"Regenerar" no necesitan ningún endpoint nuevo: Descartar es simplemente no llamar a `/apply` (nada se creó, nada que deshacer); Regenerar vuelve a llamar `POST /api/mobs/{mobId}/generate` (029) reutilizando la misma imagen de referencia ya subida.
  - **Decisión de alcance real, VoBo explícito del PO vía `AskUserQuestion`**: el AC de este ticket dice que "Usar este modelo" habilita "Editar modelo"/"Exportar", pero ningún ticket anterior (015-020, el editor manual) montó una ruta real de edición para un mob existente -- el editor solo se ejerció vía `/dev/viewport-harness` (harness de desarrollo). Se preguntó cómo resolver esta tensión; el PO eligió que 030 implemente las 3 acciones reales contra el backend y, al confirmar "Usar este modelo", navegue de vuelta a `/projects/:id` (ya real, 022) -- "Editar modelo"/"Exportar" quedan como gap explícito y documentado (nunca un botón falso) hasta que un ticket futuro monte la ruta real del editor sobre un mob existente.
  - **Verificación en vivo real completada, end-to-end contra la API real de Anthropic**: generación real completa → avance automático a "Resultado" con conteos reales (`Cuboides: 14, Bones: 7, Textura: 128×128`) y `Compatibilidad FMM: Compatible` (calculado en vivo, no un placeholder) → click "Usar este modelo" con confirmación explícita → navega de vuelta a `/projects/:id`, el mob nuevo visible en el grid real. Confirmado en Postgres: `mobs.current_revision_number=1`, `mob_revisions` con `revision_number=1`/`created_by='ai'`, `mob_drafts` con `draft_version=1` -- los 3 avanzan juntos, en una sola transacción, exactamente como pide el AC.
- **Ruta real del editor manual sobre un mob existente** (`done/034-...`): cierra un gap identificado ANTES de empezar 031 (mismo mecanismo de `AskUserQuestion` que ya resolvió tensiones similares en 027/028/030) -- ningún ticket anterior (015-020) montó una ruta productiva real para editar un mob YA EXISTENTE, solo `/dev/viewport-harness`. `GET /api/mobs/{mobId}` (nuevo, `MobDetailController`/`MobService.get`) es una ruta ya prevista desde el bootstrap del proyecto, sin `projectId` en el path. `MobEditor.vue` (`/projects/:projectId/mobs/:mobId/edit`) carga el draft real (`GET /api/mobs/{mobId}/draft`, 020, primer cliente frontend) en los componentes YA reales del editor (016-018) -- `DRAFT_NOT_FOUND` es un caso ESPERADO (mob sin ningún commit todavía), resuelto arrancando desde `emptyMobProjectModel` (nuevo helper compartido, extraído de `ai/generationEvents.ts` para no duplicar la misma lógica entre el pipeline de generación IA y el editor manual). Desbloquea 031 (edición IA)/032 (exportación)/033 (Playwright E2E), los tres asumían esta pantalla en su AC sin que ningún ticket la hubiera construido. Verificado en vivo: mob sin draft → editor vacío → Guardar real (`mob_revisions.created_by='user'`, distinto de `'ai'`); mob con draft real (el rig de IA de la verificación de 030) → carga la jerarquía y geometría completas; mob inexistente → error explícito, nunca un editor roto.
- **Edición conversacional por IA sobre un mob existente** (`done/031-...`, HU-17/HU-18, master prompt §9.3): nuevo paquete `aiorchestrator.edit`, reutiliza el `StructuredReasoningProvider` (025) pero de forma **síncrona** -- a diferencia de 028/029, editar un modelo YA existente es una sola llamada rápida sin necesidad de SSE. `AiGeometryEditPlannerService` (prompt propio, `edit-planner-v1`, enumera la misma whitelist de 9 operaciones que `GeometryPlannerService` pero framed para EDITAR -- los `target` deben ser ids REALES ya existentes, solo los `createXxx` usan `tempId`) llama al proveedor, deserializa como `RawEditPlan`, y aplica vía `GeometryEngine.apply` (005) sobre el draft ACTUAL del mob (no un modelo vacío como en generación). `AiEditService.requestPlan` NUNCA toca `mob_drafts`/`mob_revisions` -- solo persiste `ai_jobs` (`job_type='edit'`) con `base_revision_number`/`base_draft_version` capturados ANTES de llamar a la IA; `AiEditService.applyEdit` confirma la propuesta, rechazando con 409 (`StaleEditBaseException`) si el draft/revisión base avanzaron desde entonces (otro autosave/"Guardar" mientras el usuario revisaba el plan). El diff de "qué cambió" (`ChangedElement[]`, para el "La IA modificará:" del mockup) reutiliza VERBATIM `GenerationPreviewDiff` (029) contra el modelo antes/después.
  - **Bug real encontrado por testing (no por Sonar) y corregido**: `fk_ai_jobs_base_revision` (FK compuesta, 003) exige que `base_revision_number` referencie una fila REAL de `mob_revisions` -- las revisiones arrancan en 1, así que un mob con `current_revision_number=0` (con draft autosaveado pero SIN ningún "Guardar" real todavía, estado alcanzable a mitad de edición manual) no puede tener `base_revision_number=0`, violando la FK con un 500. Se agregó `NoBaseRevisionException` (400 `NO_BASE_REVISION`), chequeada al INICIO de `requestPlan` antes de llamar a la IA (falla rápido, no desperdicia una llamada real). Surgió además un segundo caso distinto: revisión real pero SIN ningún draft (si "Guardar" se llamó sin autosave previo) -- ya cubierto por `DraftNotFoundException` (404) existente desde 020, ahora con un test dedicado.
  - **Frontend** (`frontend/src/editor/`): `AiEditPanel.vue` (instrucción → plan → Antes/Después → Aplicar/Cancelar) NUNCA monta su propio viewport Three.js -- `ThreeViewportService` es un singleton deliberado (008 AC#3) que `MobEditor.vue` ya usa para el canvas editable real; el panel emite `preview-model-changed` y es `MobEditor.vue` quien decide montar `GenerationPreviewViewport.vue` (029) o `ThreeViewport.vue` en el canvas principal. Ver `docs/COMPONENTES.md` para el detalle de UI.
- **Pantalla de exportación** (`done/032-...`, HU-19, mockup 11): nuevo paquete `project.export`. `MobExportService.getStatus` calcula `hasUnsavedChanges` comparando el draft actual contra la última revisión guardada por igualdad de valor de `MobProjectModel` (mismo mecanismo que el dirty-check de autosave, 020) -- y `fmmCompatible`/`fmmIssues` SIEMPRE contra esa última revisión guardada, nunca el draft (decisión confirmada explícitamente con el PO: el panel describe un artefacto real ya exportable, nunca un estado hipotético que "Exportar última versión guardada" podría no producir). `MobExportService.exportBbmodel` (AC #1) lee exclusivamente `mob_revisions` -- "Guardar y exportar" no tiene lógica propia de guardado: el frontend orquesta `POST .../revisions` (020, "Guardar") y RECIÉN DESPUÉS `GET .../export/bbmodel`, dos llamadas sucesivas sobre endpoints ya existentes en vez de un endpoint compuesto nuevo. Descubierto durante la implementación: `ResponseEntity<byte[]>` con `Content-Type: application/json` hace que Spring prefiera `MappingJackson2HttpMessageConverter` sobre `ByteArrayHttpMessageConverter` y codifique el archivo como string base64 dentro de un JSON en vez de servirlo crudo -- fix: `application/octet-stream` (también más correcto semánticamente: es una descarga de archivo, no una respuesta JSON para consumir programáticamente). Frontend (`frontend/src/editor/ExportScreen.vue`, ruta propia `/projects/:projectId/mobs/:mobId/export`, enlazada desde un botón nuevo en `MobEditor.vue`): dueño de su propio fetch (mismo patrón que `MobEditor.vue`), sin viewport 3D (reutiliza el thumbnail YA generado por 023, evitando por completo el problema del singleton de `ThreeViewportService` que 031 tuvo que resolver explícitamente).
- **Suite E2E de aceptación** (`done/033-...`, HU-23, **gap de CI aceptado explícitamente por el PO -- ver abajo**): `scripts/e2e.sh` (uso local) orquesta el stack real completo (Docker Compose Postgres+MinIO, backend con `AI_VISION_PROVIDER=mock`/`AI_REASONING_PROVIDER=mock`, frontend) y corre `frontend/e2e/technical-alpha-acceptance.spec.ts` (Playwright) -- crear proyecto → mob por IA → "Usar este modelo" → editar a mano y Guardar → editar por IA y Aplicar → exportar `.bbmodel` v5 real, verificando el archivo descargado de verdad. Verificado reproducible en verde, corrido localmente múltiples veces contra el stack real. La mitad v4 del AC (validar contra las fixtures reales de Blockbench, 012/014) se cubre en un test de integración BACKEND aparte (`E2eAcceptanceBbmodelExportTest`) -- decisión confirmada con el PO: el producto nunca expuso una opción de exportar v4 (ni mockup ni endpoint), así que no hay ningún paso de navegador que la ejerza.
  - **Tres hallazgos reales de conformidad, solo detectables al ejercitar el flujo completo de punta a punta (nunca con los tests unitarios/integración existentes, cada uno mockeaba su propio escenario aislado)**: (1) `MockReasoningProvider` (025) tenía un ÚNICO default literal (el ejemplo de EDICIÓN del master prompt §9.3) compartido por los dos llamadores reales de la interfaz -- generación (`GeometryPlannerService`, sobre un modelo VACÍO) y edición (`AiGeometryEditPlannerService`, sobre un modelo YA existente); un servidor real recién levantado sin ningún test-harness que llame `setNextResponse` fallaría el paso de generación de verdad. Fix: dos defaults, elegidos por `promptVersion`. (2) Aun separados, el default de edición seguía apuntando a ids LITERALES (`hand_right`/etc.) que solo existen en el fixture de `AiGeometryEditPlannerServiceTest` armado a mano -- `GeometryEngine.applyCreateCuboid` SIEMPRE asigna un UUID nuevo al crear (nunca el `tempId` original), así que ningún cuboid generado (mock o Claude real) termina llamándose literalmente así. Fix: el mock ahora extrae por regex el id REAL del primer cuboid del `userPrompt` (que ya incluye el modelo actual serializado) cuando el modelo no tiene el fixture literal de 031 -- preserva el comportamiento exacto de los tests existentes, y funciona contra cualquier modelo real. (3, en `FmmCompatibilityValidator`, 013) el validador asumía SIEMPRE la forma v5 de `outliner` (groups separados) -- nunca se había corrido contra la salida de `BBModelExporterV4` (014, groups embebidos), así que cada bone de un v4 real disparaba un falso `OUTLINER_REFERENCE`. Fix: mismo criterio de detección (¿tiene `name` inline?) ya usado en el parser test-only de 012.
  - **Bloqueo real de CI, NO resuelto, aceptado explícitamente por el PO (ticket 033 cerrado en `done/` con este gap documentado, no oculto)**: se intentó wirear la suite en el `Jenkinsfile` a fondo -- 6 rondas de CI real, 5 hallazgos de infra resueltos uno por uno (permisos de `playwright install --with-deps`, colisión de puerto fijo de MinIO -- ver hallazgo #4 de la nota de 033 en `done/`, Chromium sin librerías de sistema para lanzarse, `--network host` vs. `--network container:<agente>` en la topología Docker-outside-of-Docker del agente) -- hasta toparse con un preflight CORS que vuelve sin cabeceras específicamente cuando el navegador corre en el contenedor hermano de Playwright, incluso haciendo explícito el CORS del backend. Nunca reproducido en desarrollo local. Se le presentó la situación al PO; decidió detener la iteración a ciegas contra CI, y el 2026-09-09 decidió explícitamente aceptar el riesgo y cerrar el ticket con el gap tal cual, en vez de seguir invirtiendo en el diagnóstico. **La etapa de CI se removió del `Jenkinsfile`** -- dejarla habría bloqueado el CI de todos los PRs futuros del proyecto por un problema de infra ajeno a cada PR. Mismo criterio que el gap de Sonar del ticket 008: real, documentado, sin ocultar, con decisión explícita del PO de no bloquear el cierre del Technical Alpha por él (retomable como ticket nuevo si `platform` decide investigarlo más adelante).
- **CI**: `Jenkinsfile` en la raíz corre `buildAndTest` de frontend (lint+test+build+Sonar) y backend (`./gradlew build sonar`, incluye Testcontainers) en el mismo pipeline. La suite E2E de 033 NO corre acá -- ver el punto de arriba. **Gap conocido (ver ticket 008, sin arreglar, requiere VoBo sobre `platform`):** el pipeline solo verifica realmente el Quality Gate del ÚLTIMO análisis Sonar enviado (backend) — el del frontend nunca gatea el build aunque esté en rojo. Verificar ambos manualmente contra `sonarqube-db` en cada PR hasta que se resuelva.
- **Despliegue real a la VM compartida** (`done/035-...`): conecta Galgoth Studio a la misma infraestructura que `auth-core-mc`/`mail-core-mc`/`texture-studio-mc` (Jenkins Shared Library `platform`, Traefik, Vault). Una sola imagen (`backend/Dockerfile`, multi-stage) sirve la SPA de Vue Y la API desde el mismo puerto -- el contexto de build fijo de `corePipeline` (`./backend`) nunca ve `frontend/`/`contracts/`, así que el `Jenkinsfile` aterriza ahí el bundle de producción del frontend (`VITE_API_BASE_URL` vacío -- mismo origen que el backend) y `contracts/schemas` (lo necesita `processResources` de `build.gradle`) justo antes de invocar el build de imagen. `SpaResourceConfig` (`PathResourceResolver`) sirve `index.html` como fallback SOLO cuando el archivo pedido no existe de verdad -- un primer intento con una regex por convención de nombres capturaba también los assets reales (servidos como `text/html` en vez del JS real), corregido antes de desplegar. Subdominio de tercer nivel confirmado por el PO: `studio[.-qa][-dev].galgoth.64bitstudio.com` (bajo `galgoth.64bitstudio.com`, que YA apuntaba a la VM de Diana/Minecraft -- confirmado explícitamente con el PO que este subdominio en particular va a la VM de plataforma, no a esa otra). Claude real (no mock) en los 3 ambientes, decisión explícita del PO pese al costo real que implica.
  - **Hallazgo real (primer deploy a DEV): colisión de puerto con la infra compartida misma.** El bloque de puertos reservado (PROD 8089/DEV 8090/QA 8091, siguiendo la secuencia de los 3 cores anteriores) nunca se cruzó contra los puertos de Jenkins/Vault/SonarQube/Traefik -- 8090 ya lo publica Jenkins mismo (`127.0.0.1:8090`), nunca documentado en la tabla "Convenciones de la VM" de `auth-core-mc` (que solo listaba los cores de aplicación). `docker compose up -d` falló con `port is already allocated`. Corregido: DEV→8091/QA→8092 (PROD 8089 no colisionaba). La tabla se corrigió en `auth-core-mc#95` con la lista completa de puertos de infra compartida, para que el próximo core no repita el mismo hallazgo a ciegas.
  - **Verificado end-to-end contra la VM real, desde fuera de la VM**: DNS real (Cloudflare, 3 registros A sin proxy → `159.54.153.37`), certificado TLS real emitido por `certbot` para los 3 subdominios, `https://studio-dev.galgoth.64bitstudio.com` y `https://studio-qa.galgoth.64bitstudio.com` responden 200 en `/actuator/health`, sirven la SPA real (deep-link sin 404, asset real como `text/javascript`) y `/api/projects` real -- verificado con `curl` externo, no solo "debería funcionar". `DB_PASSWORD` real en Vault (`secret/galgoth-studio/{dev,qa,prod}`, vía el AppRole `platform-admin`, sin tocar el token root) + `ANTHROPIC_API_KEY`/credenciales de MinIO reales en `/home/ubuntu/secrets/galgoth-studio/.env.{dev,qa,prod}` (no cubiertas por el patch-list de Vault de `corePipeline.groovy`, hoy hardcodeado a `DB_PASSWORD`/`VAULT_SECRET_ID` -- mismo criterio ya usado por `RESEND_API_KEY`/`TWILIO_*` en `auth-core-mc`: se llenan a mano, una sola vez, directo en el archivo real de la VM). QA promovido por Claude (`dev`→`qa`, fast-forward) tras verificar DEV -- **PROD queda pendiente de la aprobación manual de Marco** (`corePipeline` ya pausó el pipeline en el gate `input` exclusivo de él, timeout de 7 días -- comportamiento esperado, no un bloqueo).

**Fase 3 — Editor de textura/UV + generación de textura por IA** (VoBo final del PO, 10 sep 2026, ver `docs/definiciones/galgoth-studio-fase3-textura.md`; desglosada en tickets 040-056, milestones M7-M10):

- **Contrato de datos `UvRegion.status`/`UvReservation`** (`done/040-...`, Diseño técnico §1): `UvRegion` gana `status` (`UNPAINTED`/`PAINTED`/`ORPHAN`); `UvLayout` gana `reservations: List<UvReservation>` como tombstone explícito del espacio de atlas abandonado por un resize destructivo confirmado (aún sin lógica de negocio que lo produzca -- llega en 041). Ambos campos aditivos, con constructor compacto que normaliza `null` (JSON legacy de Fase 1+2 sin estos campos) a los defaults seguros, sin excepción ni migración de datos, y un constructor de conveniencia de 3 args para que `AlphaAutoPackStrategy`/`GeometryEngine`/`BBModelExportSupport`/`MobGenerationService` sigan compilando sin tocarlos.
  - **Hallazgo real**: hacer los campos requeridos en el espejo TypeScript (fiel a que el backend siempre los resuelve) rompió el type-check en 16 sitios fuera del alcance nominal (mocks de test, `emptyMobProjectModel.ts`, `autoUv.ts`) -- corregidos mecánicamente, sin lógica de negocio, para mantener `vue-tsc -b` en verde.
  - Nuevo `commitExternalModel()` en `draftModelStore.ts`: commit genérico vía el mismo mecanismo de Command que cualquier operación existente (push de `previous`, sin lógica de negocio) -- plumbing explícito para que 041/043 confirmen cambios validados por el backend, necesario también para probar el AC de Undo sobre `reservations` de este ticket.
- **`UvLayoutSelector`/`StableUvStrategy`/`BoxUvMath`** (`done/041-...`, Diseño técnico §2): `UvLayoutStrategy` ya NO tiene una única implementación (corrige la nota de 006) -- gana una sobrecarga `default` de 4 argumentos (recibe el `UvLayout` previo COMPLETO, no solo `regions()`) que en `AlphaAutoPackStrategy` delega en la de 3 argumentos existente, cero cambios de comportamiento. La matemática de box-unwrap/footprint de `AlphaAutoPackStrategy` se extrajo a `BoxUvMath` (helper compartido, `domain/uv/`) para que `StableUvStrategy` la reutilice sin duplicarla ni divergir. `StableUvStrategy` (nueva) implementa los 3 casos del Diseño técnico §2 sin reflowear cuboids no tocados: **Add** busca hueco en el "espacio verdaderamente libre" (atlas menos la unión de TODOS los `rect` de `regions` -- cualquier status -- menos TODOS los `rect` de `reservations`), `UvAtlasOverflowException` si no cabe, sin crecer el atlas ni reempaquetar nada existente; **Resize** que cambia el footprint de una cara ya `PAINTED` lanza `PaintedRegionResizeConfirmationRequiredException` (nueva, misma familia que `UvAtlasOverflowException`) sin mutar nada -- confirmado (flag `confirmPaintLoss`, todavía sin canal formal desde un endpoint, eso es wiring de un ticket posterior), crea una `UvReservation(rect viejo, RESIZE_ABANDONED)` por cada cara `PAINTED` afectada y reempaqueta el bloque en espacio libre, `UNPAINTED` en la ubicación nueva; **Delete** marca `ORPHAN` las 6 filas existentes de `UvRegion` del cuboid eliminado, sin crear ninguna reserva. `UvLayoutSelector` (nueva, `@Primary`) decide entre `AlphaAutoPackStrategy` y `StableUvStrategy` según si `previousLayout.regions()` tiene algún `PAINTED`/`ORPHAN` -- se inyecta automáticamente (autowire-by-type) en `GeometryEngine.apply`, `GeometryPlannerService` y `AiGeometryEditPlannerService`. El exportador (`BBModelExporterV5`/`V4` y sus 3 callers -- `MobExportService`, `MobGenerationService`, `GenerationResultService`) queda deliberadamente FUERA: usan `@Qualifier("alphaAutoPackStrategy")` para no heredar el nuevo `@Primary` por accidente vía autowire-by-type (Hallazgo A revertido del Diseño técnico §2/§3 -- el ticket 044 le quitará el parámetro de estrategia por completo).
  - **Hallazgo real, corregido en este ticket**: `GeometryEngine.apply(model, ops, uvLayoutStrategy)` invocaba la sobrecarga de 3 argumentos (nunca pasaba `previousLayout`) y `touchesUv` no incluía `removeCuboid` -- un batch de solo-delete nunca llegaba a invocar la estrategia, haciendo imposible el caso "Delete marca ORPHAN" a través del motor real. Corregido: ahora pasa `model.uv()` (el layout ANTES del batch) y `touchesUv` incluye `removeCuboid`. Además, `GeometryEngine.apply(model, ops)` (3 args, sin estrategia) perdía en silencio `reservations` en CUALQUIER operación -- incluso una que no toca UV -- porque construía el `UvLayout` resultante con el constructor de 2 argumentos (default a lista vacía); corregido para preservar `model.uv().reservations()` sin cambios. `UvLayoutStrategy.Result` gana un campo `reservations` aditivo (mismo patrón de constructor compacto + convenience de 2 args que `UvRegion`/`UvLayout` en el 040) para que la reserva que crea `StableUvStrategy` sobreviva el viaje de vuelta a través de `GeometryEngine`.

- **`TexelDensity`/`AtlasResolutionCalculator`/`TexelDensityUpgrade`** (`done/042-...`, Diseño técnico §7, cerrado definitivamente por el PO 10 sep 2026): `x1`/`x2` son perfiles de densidad de texel (1 o 2 texels/unidad), NUNCA una dimensión de atlas -- `AtlasResolutionCalculator.computeAtlas` empaqueta el footprint de TODOS los cuboids a la densidad elegida (reutilizando `AlphaAutoPackStrategy.packedBoundsOf`, expuesto paquete-visible, sin duplicar el shelf-packing) y elige la potencia de 2 inmediatamente contenedora -- el atlas es SIEMPRE consecuencia del packing, nunca un valor fijo. `BoxUvMath.footprintOf`/`boxUnwrapFaces` ganan una sobrecarga aditiva con `TexelDensity` (las viejas sin densidad delegan en `X1`, cero cambio para 006/007/041). `TexelDensityUpgrade.upgradeTo` reempaqueta TODO a la densidad nueva (real, no solo el lienzo) antes de que exista `PAINTED`; después, es un no-op explícito (`Result.applied()=false`) -- nunca un cambio silencioso ni una excepción.
  - **Hallazgo real, corregido a mitad de camino**: el primer diseño calculaba el atlas más grande a X2 pero seguía empaquetando las caras reales a X1 -- un "upgrade" falso (mismo detalle de siempre, solo más lienzo vacío). Corregido extendiendo `BoxUvMath`/`AlphaAutoPackStrategy` para que el upgrade también reempaquete con densidad real.
  - **`GeometryPlannerService.applyOperations` ya no confía en el `TextureDocument` fijo (128×128 hardcodeado) del modelo de arranque**: aplica la geometría en dos pasadas -- (1) sin UV, para conocer los cuboids reales que la IA propuso; (2) con el atlas correcto ya calculado a densidad X1 ANTES de invocar la estrategia real de UV.
  - **Gap documentado, no resuelto (fuera de alcance explícito de 042)**: ni `MobProjectModel` ni `UvLayout` persisten qué densidad está vigente -- el upgrade a X2 es una foto única; una operación de geometría posterior por el camino estándar recalculará a X1 implícito. Un ticket futuro necesita el campo de densidad persistido, cableado a través de todo el pipeline, antes de exponer el upgrade en el wizard.
  - **Coordinación real de esta sesión**: se detectó un commit de otra rama (fix de Sonar de 041) mezclado accidentalmente por compartir working directory sin `isolation: "worktree"` entre agentes -- reconciliado (cherry-pick a la rama correcta de 041, limpieza de la rama de 042). Desde este ticket, los agentes de implementación de tickets se lanzan con worktree aislado.

- **`BBModelExporterV5`/`V4.export(model)` sin `UvLayoutStrategy` + `LegacyUvNormalizationService`** (`done/044-...`, Diseño técnico §3, reversión directa del Hallazgo A anotado en 041): el exportador pierde el overload de 2 argumentos -- `export(model)` es ahora el ÚNICO método, nunca invoca `UvLayoutSelector`/`AlphaAutoPackStrategy`/`StableUvStrategy` (cero dependencia de `domain.uv`, garantía estructural, no solo de comportamiento) y serializa EXACTAMENTE `model.uv()`/`model.texture()` tal como llegan, incluso sin ninguna región pintada (antes de este ticket SIEMPRE recomputaba). Los 3 callers (`MobExportService`, `MobGenerationService`, `GenerationResultService`) pierden por completo el campo/`@Qualifier("alphaAutoPackStrategy") UvLayoutStrategy` que 041 les había dejado deliberadamente aislado -- ninguno de los tres lo usaba para nada más que exportar. Nuevo `LegacyUvNormalizationService` (`domain/uv/`): no-op salvo que se cumplan AMBAS (a) ninguna región `PAINTED`/`ORPHAN` y (b) el `UvLayout` almacenado difiere ESTRUCTURALMENTE (mismo `Set<UvRegion>`) de lo que `AlphaAutoPackStrategy.layout(cuboids, w, h)` calcularía hoy -- cuando normaliza, el `MobProjectModel` recalculado es transitorio, solo en memoria para esa llamada, nunca persistido de vuelta a la Revision. Invocado exclusivamente por `MobExportService` (único caller real de exportación desde una Revision persistida) ANTES del exportador -- `MobGenerationService`/`GenerationResultService` exportan modelos frescos del propio pipeline de esta sesión (nunca una Revision legacy), así que no lo necesitan.
  - **Hallazgo real, corregido en el camino**: la textura placeholder embebida (ticket 011, mecanismo ortogonal a la UV, sin cambios de fondo) usaba `UUID.randomUUID()` -- inofensivo mientras el exportador recomputaba UV en cada export (nada dependía de bytes estables), pero incompatible con la nueva garantía de determinismo de `export(model)` al ser ahora el único overload (AC del ticket: "exportar el mismo modelo dos veces produce bytes idénticos"). Corregido a un UUID fijo (`BBModelExportSupport`); el golden snapshot (`BBModelExporterV5SnapshotTest`, ticket 012) se regeneró para incluir esa entrada de `textures` (antes `[]`, porque su fixture ya traía `texture:0` en las caras desde antes de este ticket).
  - **Reescrito, no silenciado**: `BBModelExporterV5PlaceholderTextureTest` (ticket 011) probaba exactamente el comportamiento que este ticket revierte (recompute + `UvAtlasOverflowException` AL EXPORTAR) -- se reescribió para el contrato nuevo (modelos que llegan con la UV ya resuelta, como los entregaría hoy `GeometryEngine`/`UvLayoutSelector`) en vez de eliminarlo o debilitarlo.
- **`POST /api/mobs/{mobId}/geometry/apply` -- cierre del Hallazgo B para Resize/Add/Remove** (`done/043-...`, Diseño técnico §2/§15): `MobGeometryController`/`MobGeometryApplyService` (`project/api`/`project/geometry`, mismo patrón que `project/draft`) hacen que el backend sea la autoridad real de geometría/UV para las 3 operaciones que la afectan -- whitelist cerrada (`createCuboid`/`resizeCuboid`/`removeCuboid`, cualquier otra op responde 400 `UNSUPPORTED_GEOMETRY_OPERATION`), reutiliza `DraftPersistenceService.getDraft`/`autosave` tal cual (mismo `draft_version`, mismos 404 ya establecidos) en vez de reimplementar persistencia. **Wiring de `confirmPaintLoss`, lo que 041 dejó explícitamente pendiente**: `UvLayoutStrategy` gana una sobrecarga `default` de 5 argumentos (ignorada por `AlphaAutoPackStrategy`, real en `StableUvStrategy` -- que ya la tenía desde 041 como método propio, ahora `@Override`); `UvLayoutSelector` la propaga a la estrategia resuelta; `GeometryEngine.apply(model, ops, uvLayoutStrategy)` (3 args) delega en un nuevo `apply(..., confirmPaintLoss)` (4 args) con `false` -- cero cambio para `GeometryPlannerService`/`AiGeometryEditPlannerService`. `PaintedRegionResizeConfirmationRequiredException` se traduce a 409 (`details: ["cuboidId:face", ...]`); `UvAtlasOverflowException`/`GeometryValidationException` cruzan la frontera HTTP por primera vez (400).
  - **Frontend**: nuevo `geometryApplyStore.ts` (Pinia) centraliza Resize/Add/Remove server-side -- un éxito o confirmación aplican el resultado vía `draftModelStore.commitExternalModel` (el hook que 040 dejó preparado exactamente para esto). `ThreeViewport.vue` (gizmo 'scale') e `InspectorPanel.vue` ("Tamaño") disparan la llamada SOLO al soltar/confirmar, nunca durante el preview local (`objectChange`/cada tecla) -- Move/Rotate sin cambios, 100% client-side. Un `ConfirmDialog` (design system, reutilizado tal cual) montado una vez en `MobEditor.vue` resuelve el modal de confirmación de pérdida de pintura para los 2 puntos de entrada de Resize.
  - **Gap real, señalado explícitamente y dejado fuera de alcance**: "Duplicar cuboid" es internamente un `createCuboid` y hereda el mismo Hallazgo B que este ticket cierra para "Agregar" -- el ticket no lo nombra, así que se dejó sin tocar (sigue 100% client-side); pendiente de una decisión del Product Owner sobre si amerita ticket propio.
  - **Revisión visual en vivo pendiente**: Claude in Chrome no estaba disponible en el entorno de esta implementación para ejercer el modal de confirmación contra la app corriendo -- verificado solo con tests automatizados; queda pendiente una pasada visual antes de considerar la UI del modal 100% validada.

- **`TexturePatchCommand`/`textureEditorStore.ts`** (`done/046-...`, Diseño técnico §9, frontend): nuevo mecanismo de Undo/Redo del editor de textura, basado en patches acotados al `rect` mínimo tocado -- NUNCA un snapshot del atlas completo por trazo (inviable en memoria, un atlas de 256x256 son 256KB+ sin comprimir por Command). `TexturePatchCommand { rect: {x,y,width,height}, beforePixels, afterPixels }` (`frontend/src/editor/texture/TexturePatchCommand.ts`) guarda únicamente los píxeles RGBA de `rect`, antes y después. Nuevo store Pinia `textureEditorStore.ts`, **100% independiente de `draftModelStore.ts`** (018/019, geometría) -- no la importa, no comparte módulo ni estado; Ctrl+Z en el tab Textura nunca toca el draft de Modelo y viceversa (verificado con un test cruzado explícito instanciando ambos stores). `recordPatch(rect, beforePixels, afterPixels)` aplica `afterPixels` sobre el atlas vigente y empuja el Command a Undo, limpiando Redo (mismo comportamiento estándar que `draftModelStore.recordCommand()`); `undo()`/`redo()` escriben in-place solo el área de `rect` sobre el atlas -- O(área del rect), nunca O(atlas completo).
  - **Decisión de diseño -- de dónde sale el bitmap sobre el que se aplican los patches**: el canvas real del editor de textura no existe todavía (ticket 047) pero aplicar/deshacer un patch necesita algo concreto sobre lo que escribir píxeles, así que el store mantiene el **atlas completo en memoria como estado** (`atlas: { width, height, pixels: Uint8ClampedArray }`, mismo layout RGBA que `ImageData.data`), expuesto vía `loadAtlas()`/`readRegion()` para que 047 lo alimente y capture `beforePixels` antes de pintar. Esto no viola el AC de tamaño acotado: lo proporcional al `rect` es cada Command HISTÓRICO, no el estado actual (una sola copia del atlas es inevitable, igual que `MobProjectModel` completo en `draftModelStore`). `atlas` y ambas pilas usan `shallowRef` (no `ref` como `draftModelStore`) -- son buffers binarios grandes donde la reactividad profunda de Vue por índice no aporta nada; se llama `triggerRef` explícitamente tras mutar el buffer in-place.
  - **Divergencia deliberada del patrón de `draftModelStore`**: ahí cada Command guarda el `MobProjectModel` completo anterior (barato porque las operaciones son puras); acá NUNCA se guarda una copia del atlas completo por Command, solo los píxeles del `rect` -- es exactamente el problema que este ticket resuelve.
  - **Aclaración sobre el alcance real de los AC (sin parches silenciosos)**: los AC del ticket están redactados en términos de herramientas que todavía no existen (`pointerdown`/`pointermove`/`pointerup` de un trazo, uso de la Cubeta) -- no hay canvas ni herramientas de pintado en este ticket (son el 047). Un trazo completo se simula como UNA sola llamada a `recordPatch()` -- el store no distingue de dónde viene el `rect`/`beforePixels`/`afterPixels`, así que el mismo mecanismo cubre trazo/fill/paste/Apply de IA sin necesidad de fabricar una herramienta "Cubeta" simulada fuera de alcance.
  - Frontend: 388 tests, 0 failures (+9 nuevos: `textureEditorStore.spec.ts`, `textureEditorStore.crossStoreIndependence.spec.ts`). `vue-tsc -b` y `eslint --max-warnings 0` sin hallazgos.

- **Persistencia content-addressed de textura + backend autoridad de `storageKey`** (`done/045-...`, HU-30/HU-31, Diseño técnico §4/§6): `PUT /api/mobs/{mobId}/texture` (`MobTextureController`/`TextureService`, paquete nuevo `project.texture`) recibe bytes crudos de un PNG (mismo estilo que el thumbnail del 023, sobre `AssetStorageService` ya existente sin cambios de configuración) -- decodifica/valida con `ImageIO` (400 `INVALID_TEXTURE` si no decodifica, nunca confía en el `Content-Type` declarado), **re-codifica a PNG canónico y hashea/almacena esos bytes canónicos, no los bytes crudos subidos** (decisión explícita: dos encoders/clientes distintos pueden producir bytes PNG distintos para el mismo contenido de píxeles, lo que rompería el dedup automático de HU-31 si se hasheara el crudo) y sube a MinIO bajo `textures/{sha256-hex}.png` solo si la key no existe ya (`AssetStorageService.exists`, método nuevo vía `HeadObjectRequest`). El contrato de respuesta (`{storageKey}`) no tiene ningún campo para que el cliente proponga un `storageKey` propio -- "el backend es la única autoridad" es una garantía estructural del contrato, no solo de comportamiento verificado en runtime.
  - **Flush obligatorio antes de crear una Revision (defensa en profundidad)**: `DraftPersistenceService` gana `requireTextureAssetPersisted(model)`, invocado en AMBOS métodos que escriben `mob_revisions` (`saveRevision` -- "Guardar" -- y `applyGenerationProposal` -- "Usar este modelo"/Apply, 030) -- si `model.texture().storageKey()` no es `null`, verifica su existencia real en MinIO antes de construir la entidad; si no existe, lanza `DanglingTextureReferenceException` (nueva, mismo estilo que `DraftNotFoundException`/`UvAtlasOverflowException`) sin escribir ninguna fila. El "await" del lado cliente antes de invocar Guardar queda explícitamente fuera de alcance (backend-only por diseño de este ticket) -- esta verificación es la garantía de que un bug del cliente que no respete ese orden nunca produce una referencia colgante persistida.
  - **Dirty-check de autosave (HU-30) confirmado sin cambios**: `TextureDocument.storageKey` es un `String` más dentro de `MobProjectModel` -- el `equals()` de records que `DraftPersistenceService.autosave` ya usa (ticket 020) lo compara automáticamente como cualquier otro campo de valor, O(1), sin comparar bitmaps. Dedup entre revisiones consecutivas (HU-31) también se cumple por construcción de las dos piezas de arriba, sin código de deduplicación adicional: el mismo bitmap siempre produce el mismo `storageKey`, y `saveRevision` ya compara el modelo completo contra la última revisión antes de decidir si crea una fila nueva.

- **`TexturePlan` -- análisis de material/paleta vía Claude, paso separado ANTES de OpenAI** (`done/052-...`, HU-36, Diseño técnico §11 punto 1): nuevo contrato `domain/model/TexturePlan` (+ `BoneSemanticLabel`, `TexturePalette`, `FaceMaterialNote`) -- equivalente de `ModelIntent` (028) para el paso de textura: etiqueta semántica por bone, paleta dominante/acento GLOBAL de la imagen, y notas de material por (bone, cara `FaceName`). `boneId`/`boneName` referencian siempre un `Bone` REAL del modelo ya generado en Fase 1+2 (a diferencia de `ModelIntent`, que arranca de un modelo vacío) -- mismo criterio de "ids reales, nunca inventados" que `AiGeometryEditPlannerService` (031). Nuevo `contracts/schemas/texture-plan.schema.json` + `contracts/fixtures/texture-plan-example.json`, y `modelvalidation/TexturePlanValidator` (mismo patrón exacto que `ModelIntentValidator`, sobre el árbol JSON crudo, ANTES de deserializar). Nuevo `aiorchestrator/texture/TexturePlanService.analyze(imageBytes, contentType, currentModel)`: arma el prompt (imagen + modelo actual serializado), invoca al proveedor, valida contra el schema y devuelve `TexturePlanAnalysisResult(TexturePlan, AiProviderResponse)` -- una respuesta inválida lanza `InvalidTexturePlanException` y el flujo se detiene ahí, sin generar ninguna imagen (mismo criterio que 028). Sin cambios a `ai_jobs` en este ticket -- el servicio devuelve los mismos 4 campos de trazabilidad (`provider`/`model`/`promptVersion`/`schemaVersion`) ya empaquetados en `AiProviderResponse` para que el orquestador de generación de textura (054, todavía no existe) los persista, igual que hace `MobGenerationService` con el resultado de `VisionAnalysisService`.
  - **Hallazgo real, señalado explícitamente (sin parche silencioso) -- el Diseño técnico §11 y el propio ticket 052 nombran mal la interfaz**: ambos dicen `StructuredReasoningProvider`/`MockReasoningProvider` para este paso, pero esa interfaz es puramente de TEXTO -- `ReasoningRequest` no tiene ningún campo de imagen (su propio Javadoc, ticket 025, lo dice explícitamente) y `ClaudeReasoningProvider` delega en `ClaudeMessagesClient.callWithText`, que nunca arma un content-block `image`. Analizar de verdad la imagen de referencia (AC #1 del ticket) requiere el mismo mecanismo multimodal que ya usa `ModelIntent`/`VisionAnalysisService` (028) -- `VisionModelProvider.analyzeReferenceImage`, respaldado por `ClaudeMessagesClient.callWithImage`. `TexturePlanService` usa `VisionModelProvider` (y, en tests, `MockVisionProvider`) en su lugar. Ninguna clase de `aiorchestrator/provider/` se tocó para llegar a esta conclusión -- ambas interfaces se usan tal cual ya existen desde 025; el cambio es solo CUÁL de las dos invoca este servicio nuevo. Reportado para VoBo explícito del Product Owner -- si se prefiere otra solución (ej. extender `StructuredReasoningProvider` con soporte de imagen), es un cambio de contrato de proveedor que necesita su propio ticket/VoBo dedicado.
  - **Fuera de alcance de este ticket, explícito**: sin espejo TypeScript (mismo criterio que `ModelIntent` -- contrato 100% backend-a-backend, consumido recién por `TextureGenerationSheetPlanner`/054, ningún consumidor frontend real hoy); sin persistencia en `ai_jobs` (el `job_type`/columna `target_bone_id` nuevos son diseño de 054); sin cruce de que un `boneId` devuelto por el proveedor corresponda a un `Bone` real del modelo recibido más allá de lo que el JSON Schema puede expresar (mismo nivel de confianza que el planner de geometría delega en la deserialización -- una validación de existencia real, si hace falta, es responsabilidad de quien consuma el `TexturePlan`, 054).
  - Backend: 325 tests, 0 fallos (+9 nuevos: `TexturePlanValidatorTest` 6, `TexturePlanServiceTest` 3).
- **`OpenAiImageProvider` -- primer proveedor real de `ImageGenerationProvider`, modelo configurable** (`done/051-...`, HU-39/HU-40, Diseño técnico §11/§12): la interfaz de 025 gana de forma ADITIVA `generateTextureSheet(TextureGenerationSheetRequest request)` (`generateImage(String)` no cambia de forma ni consumidores; el record `TextureGenerationSheetRequest(prompt, referenceImageBytes, sheetWidth, sheetHeight, style)` queda anidado en la interfaz, forma EXACTA fijada por el ticket, ajustable de forma aditiva por 053 si el pipeline completo lo necesita). `OpenAiImageProvider` (nuevo, `aiorchestrator/provider/`) llama `POST /v1/images/edits` (multipart, campo `image`) cuando `referenceImageBytes` no está vacío -- inpaint sobre esa imagen base -- y `POST /v1/images/generations` (JSON) sin referencia; ambos piden `response_format: b64_json` y decodifican `data[0].b64_json`. Mismo patrón HTTP que `ClaudeMessagesClient` (`RestClient`, sin dependencia nueva); `style` no tiene parámetro propio en el contrato genérico de OpenAI (a diferencia de `vivid`/`natural`, específico de DALL-E-3) -- se pliega como instrucción de texto al final del prompt, decisión documentada en el Javadoc de la interfaz.
  - **Modelo nunca hardcodeado (§12, cerrado por el PO)**: `ai.openai.image-model=${OPENAI_IMAGE_MODEL:gpt-image-2.5-sunburst-2026-09-08}` (snapshot fechado, mismo criterio que fijar una versión de imagen Docker por dígest en vez de `:latest`) llega por constructor y viaja tal cual en cada request -- `OpenAiImageProvider#model()` lo expone (mismo mecanismo que `ClaudeMessagesClient#model()`) para que un futuro orquestador (ticket 054, todavía no existe) lo persista en `ai_jobs.model` sin que este proveedor conozca nada de persistencia.
  - **Selección real/mock**: `AI_IMAGE_PROVIDER` (relaxed binding -> `ai.image-provider`, `AiProviderConfig`, `@ConditionalOnProperty`, default `openai`) -- mismo mecanismo exacto que `AI_VISION_PROVIDER`/`AI_REASONING_PROVIDER`. `ImageGenerationProvider` es una interfaz distinta de `VisionModelProvider`/`StructuredReasoningProvider` y ninguna clase implementa más de una a la vez, así que el hallazgo de ambigüedad de Spring documentado en `AiProviderConfig` (025) no aplica acá.
  - **`MockImageProvider` deja de ser un placeholder de interfaz**: `generateImage(String)` conserva EXACTAMENTE su comportamiento de 025 (bytes vacíos por defecto, configurable vía `setNextImage`, mismo test sin modificar). `generateTextureSheet` es la parte nueva -- sin configurar explícitamente, genera un PNG sintético determinista (tablero de ajedrez, `ImageIO`) con las dimensiones EXACTAS pedidas y un color derivado de un hash estable de `prompt`+`style` (mismo request -> mismos bytes, decodificable/verificable en cualquier test); `setNextTextureSheet(bytes)` lo hace configurable, mismo patrón que `MockReasoningProvider#setNextResponse`.
  - **Alcance explícitamente NO tocado por este ticket (sin parches silenciosos)**: `ai_jobs` no se escribe desde acá -- el pipeline completo que orquesta `TexturePlan` -> `TextureGenerationSheetPlanner` -> este proveedor -> `TextureSheetSlicer`/`TextureCompositorService` -> atlas es el ticket 054 (`TextureGenerationSheetPlanner`/slicing/composición son el 053), ninguno de los tres existe todavía. El AC del ticket sobre "cada `ai_jobs` guarda el modelo real usado en su momento, no el más reciente" se verifica a nivel de proveedor (dos instancias con `ai.openai.image-model` distinto reportan y envían cada una su propio valor, sin estado estático compartido) -- la integración real contra una fila de `ai_jobs` queda para 054, que ya tiene `OpenAiImageProvider#model()` disponible para leerla.
  - Backend: 330 tests, 0 failures (+14 nuevos: `OpenAiImageProviderTest` con 8, +4 en `MockImageProviderTest`, +2 en `AiProviderConfigTest`).

- **`TextureCanvas.vue` -- el canvas real del editor de textura y sus herramientas de pintado** (`done/047-...`, Diseño técnico §9, HU-24/HU-26/HU-27, frontend): primer componente que monta el mecanismo de Undo/Redo de 046 sobre un atlas y canvas reales. Arquitectura de dos capas superpuestas -- un `<canvas>` bitmap (tamaño intrínseco EXACTO al atlas, mostrado más grande vía CSS `image-rendering: pixelated`, nunca un zoom interactivo) y un `<svg>` overlay `pointer-events: none` (guía UV/grid/resaltado de región) que NUNCA toca `atlas.pixels` -- la grilla no puede filtrarse al bitmap real por construcción, no por un chequeo posterior. Herramientas (Pincel/Borrador/Cubeta/Eyedropper/color picker+paleta/grid toggle) implementadas como funciones puras sin ninguna dependencia de `<canvas>`/DOM (`frontend/src/editor/texture/pixelTools.ts`) -- deliberado: jsdom no implementa un contexto 2D real, así que el pintado pixel-perfect solo es testable escribiendo bytes RGBA directo sobre el `Uint8ClampedArray`. Pincel/Borrador comparten `stampSquare`/`stampLine` (Borrador = mismo trazo con `TRANSPARENT`, cero duplicación); Cubeta usa `computeFloodFill` (BFS 4-direccional real, nunca muta su entrada). Un trazo completo clona el atlas UNA vez en `pointerdown` (`textureEditorStore.readRegion()` sobre el rect completo, tal como sugería el Hecho de 046), pinta sobre esa copia durante `pointermove` (feedback en vivo sin tocar el store todavía) y llama `recordPatch()` UNA sola vez en `pointerup`; la Cubeta es atómica. `textureRectBuffer.ts` extrae `writeRectInto`/`readRectFrom` de `textureEditorStore.ts` (046) para que la Cubeta reutilice la misma mecánica de slicing sin duplicarla -- refactor interno, cero cambio de API pública ni de tests existentes.
  - **Hallazgo real no anticipado por el ticket -- el preview 3D nunca supo renderizar texturas**: `buildMobScene.ts` (008) siempre usó un material de color plano gris, sin `map` ni UV real asignada a la geometría -- el atlas nunca se había mostrado en 3D en todo el proyecto, lo que habría hecho imposible el AC de HU-26 ("el preview 3D refleja el cambio"). Corregido de forma aditiva: nuevo `frontend/src/viewport/textureUvMapping.ts` (`applyCuboidFaceUvs`) reescribe el atributo `uv` de la `BoxGeometry` de cada cuboid usando el rect real de `Cuboid.faces[x].uv` (ya calculado por AutoUv) y el orden de caras determinista de `THREE.BoxGeometry` (+x,-x,+y,-y,+z,-z -> east/west/up/down/south/north, convención Minecraft del ADR 0001) -- SIN tocar `materialIndex`/grupos de material, que es explícitamente HU-25/ticket 049 ("etiquetado de cada cara/grupo de material... en `buildCuboidMesh`"), no invadido. `buildMobGroup`/`ThreeViewportService.setModel` ganan un 3er parámetro opcional `atlasTexture` -- sin él (todos los callers ya mergeados: `ThreeViewport.vue`/`GenerationPreviewViewport.vue`), comportamiento idéntico al de siempre. La textura 3D es un `THREE.DataTexture` que envuelve DIRECTO el `Uint8ClampedArray` del atlas (`flipY = false`, `NearestFilter`, sin mipmaps) -- sin canvas 2D intermedio, 100% testable sin DOM.
  - **Segundo hallazgo real, más sutil -- `triggerRef` de un `shallowRef` de Pinia no dispara de forma confiable un `watch()` externo al store**: el diseño original de este ticket asumía un `watch(() => textureEditorStore.atlas, ...)` como única fuente de verdad para refrescar el preview tras cualquier `recordPatch()`/`undo()`/`redo()` (todos llaman `triggerRef(atlas)` internamente desde 046). Verificado en vivo con una reproducción mínima aislada: una REASIGNACIÓN real de `.value` (`loadAtlas()`) sí notifica a un `watch()` externo, pero una mutación in-place + `triggerRef()` posterior (el patrón exacto de `recordPatch()`/`undo()`/`redo()`) no lo hace de forma confiable. Corregido sin tocar el store 046 (su contrato/tests no cambian): `TextureCanvas.vue` llama explícitamente `syncDataTexture()`/`redraw()` justo después de cada `recordPatch()` que él mismo dispara, sin depender de ningún `watch()` sobre el store. **Gap señalado para el próximo ticket que cablee Undo/Redo con UI** (no este, ver abajo): debe refrescar la textura 3D/canvas explícitamente tras llamar `undo()`/`redo()`, nunca asumir que un `watch()` externo lo hace solo -- recomendación de mejora continua: documentar este gotcha de Pinia/`triggerRef` en la memoria del equipo, mismo patrón que `ui-accessibility-guard-gotchas.md`.
  - **Carga inicial (HU-24)**: `loadAtlas(model.uv.textureWidth, model.uv.textureHeight)` sin distinguir `revision_number >= 1` de un draft vacío -- mismo criterio sin código especial que 034. Decisión explícita: el atlas siempre carga EN BLANCO al tamaño real -- no existe todavía ningún backend que devuelva bytes de una textura ya pintada (`GET`/`PUT /texture` es HU-30/31, fuera de 040-046); "atlas real" acá significa "con las dimensiones reales del modelo", no "con contenido de una sesión anterior".
  - **Selector de región**: etiqueta cada `UvRegion` (excluyendo `orphan`) como `"${cuboid.name} (${face})"` -- misma convención ya usada por `MobEditor.vue` (043) para el modal de pérdida de pintura, reutilizada en vez de inventar un formato nuevo (riesgo #1 del documento de definición, sin convención fija de nombres, aceptado tal cual).
  - **Fuera de alcance, explícito**: selección cruzada cuboid<->UV (049) y selección de cuboid desde el viewport 3D; import de PNG (048); Selección/Copiar-pegar y Undo/Redo con UI propia (HU-27 los menciona en la HU más amplia, pero la sección "Qué implementar" de ESTE ticket no los lista); control de opacidad (mencionado en el mockup 07/HU-41 de la pantalla completa, ticket 050, no en las herramientas de HU-27); zoom interactivo (la escala CSS fija del canvas ya ejercita la misma conversión de coordenadas que un zoom real necesitaría, verificada con un canvas 10x escalado); ensamblado de la pantalla completa (mockup 07, ticket 050) -- este componente es la pieza que ese ticket va a montar en el tab "Textura".
  - **Revisión visual en vivo no disponible**: worktree aislado sin extensión de Chrome conectada (`list_connected_browsers` devolvió vacío, verificado en la misma sesión) -- verificado solo con tests automatizados y el chequeo estático de accesibilidad de cada commit; mismo gap ya señalado en el ticket 043.
  - Frontend: 441 tests, 0 failures (62 archivos, +53 nuevos). `vue-tsc -b`, `eslint --max-warnings 0` y `vite build` en verde.
- **`TextureGenerationSheetPlanner`/`TextureSheetSlicer`/`TextureCompositorService` -- aislamiento espacial** (`done/053-...`, HU-37/HU-40, Diseño técnico §11/§21): nuevo paquete `aiorchestrator/texture/` con `TextureGenerationSheet`/`CuboidFacePlacement` (records, forma EXACTA del Diseño técnico §11 -- `[a,b,c,d]=[x0,y0,x1,y1]` para `sheetRect`/`atlasUvRect`, misma convención que `Face#uv()`/`UvRegion#rect()`). `TextureGenerationSheetPlanner` (`@Component`, 100% determinista, sin IA) arma el/los `TextureGenerationSheet` de un bone a partir del `MobProjectModel` (UV ya resuelta por `UvLayoutSelector`/`StableUvStrategy`, 041) + el `TexturePlan` (052) -- **no reasigna UV**, el `atlasUvRect` de cada placement se LEE de `model.uv().regions()`, nunca se recalcula. El layout interno de la sheet usa un nuevo `ShelfBinPacker` (bin-packing determinista tipo shelf, package-private) que garantiza no-solape ESTRUCTURAL entre `sheetRect` de un mismo bone, separados por un gutter (`GUTTER_PX=2`, constante documentada). `TextureSheetSlicer` (`@Component`) recorta cada `sheetRect` de la imagen generada vía `BufferedImage#getSubimage` acotado EXACTAMENTE al rect -- clipping estructural, nunca lee un píxel fuera (cualquier "bleed" queda simplemente fuera de la subimagen pedida, descartado sin ningún branch especial). `TextureCompositorService` (`@Service`) compone cada slice EXCLUSIVAMENTE sobre su `atlasUvRect`, sobre una copia en memoria decodificada de los bytes del atlas (nunca el bitmap persistido en vivo, nunca muta el array de entrada), con `Graphics2D.setClip(...)` fijado EXACTAMENTE al rect de destino antes de dibujar -- ningún píxel de un placement puede modificar otra región, ni por redondeo del resampling al escalar (un slice que no calza con su `atlasUvRect` se escala automáticamente, sin reintento ni error). Nueva `TextureGenerationFailedException` (fallo técnico de imaging/IO -- decodificación/recorte, mismo estilo simple que `UvAtlasOverflowException`, sin depender de `AiProviderResponse` porque no es un rechazo de contenido de IA) se lanza ANTES de aplicar cualquier cambio, nunca deja un atlas parcialmente compuesto. `TextureSheetPromptComposer` (nuevo, utilitario estático) compone el prompt determinista con el "background/mask" del AC como descripción TEXTUAL de la grilla de rects (el `TextureGenerationSheetRequest` real de 051 no tiene canal de máscara separado).
  - **Fallback/batching explícito**: `MAX_SHEET_DIMENSION_PX=1536` (constante PROVISIONAL, documentada -- lado más largo de los presets conocidos de `gpt-image-*`, a verificar contra el modelo real cuando 054 conecte el pipeline). Cuando el layout de un bone excedería este límite, `ShelfBinPacker` lo divide en N bins deterministas y `plan(...)` devuelve `List<TextureGenerationSheet>` de tamaño N (verificado: partición completa de placements, sin pérdida ni duplicados entre sub-sheets).
  - **Decisión de diseño reportada explícitamente (identificación de parte del batching, VoBo pendiente del PO)**: el Diseño técnico §11 fija la forma EXACTA de `TextureGenerationSheet` sin campos de índice, pero el AC de batching exige que cada parte sea identificable explícitamente. Resuelto SIN agregar campos al record: el índice (0-based) de cada elemento de la `List<TextureGenerationSheet>` devuelta + el tamaño de esa lista son la identificación que 054 usará para construir `stage=generando_bone_X (parte N/M)`.
  - **Simplificación documentada**: `orientationHint` se resuelve solo desde `FaceName` (front/back/side/top/bottom) -- no compone la rotación del bone (`CoordinateSystem`/ADR 0001) como sugiere el Diseño técnico §11, porque ningún AC de este ticket (foco: aislamiento espacial, §21) exige verificar su contenido. Señalado para que el PO decida si amerita ticket propio antes de que 054 dependa de esto para algo más que texto de prompt.
  - **Explícitamente NO tocado (fuera de alcance, sin parches silenciosos)**: el pipeline orquestador completo (SSE, `ai_jobs`, diff, Apply/Reject, endpoint HTTP) es el ticket 054, todavía no existe -- estos tres componentes quedan invocables como piezas de dominio, sin wiring de API/persistencia.
  - Backend: 369 tests, 0 failures (+31 nuevos: `ShelfBinPackerTest` 6, `TextureGenerationSheetPlannerTest` 8, `TextureSheetPromptComposerTest` 5, `TextureSheetSlicerTest` 5, `TextureCompositorServiceTest` 7).
- **Face picking determinista + selección cruzada cuboid<->UV** (`done/049-...`, HU-25/HU-26, Diseño técnico §14, frontend): `buildCuboidMesh` (`buildMobScene.ts`) etiqueta SIEMPRE `mesh.userData.faceNamesByGroup = BOX_GEOMETRY_FACE_ORDER` -- reutiliza EXACTAMENTE el mismo array que ya usaba `applyCuboidFaceUvs` (047) para el orden de `THREE.BoxGeometry`, sin introducir un segundo mapeo de orientación. `ThreeViewportService.pickCuboidFaceAt(clientX, clientY)` (nuevo, comparte el raycasting NDC con `pickCuboidIdAt`/017 vía `raycastMobChildren`) resuelve `{cuboidId, face}` indexando ese array con `intersection.face.materialIndex` -- determinista, cero cálculo desde la normal.
  - **Hallazgo real, necesario para que el mecanismo del AC funcione en absoluto**: verificado contra el código fuente real de Three.js (`Mesh.js#_computeIntersections`), cuando `mesh.material` es una única instancia (no un array), Three.js NUNCA copia `group.materialIndex` al resultado del raycast -- lo deja hardcodeado en `0` sin importar qué cara real se golpeó (esa asignación solo ocurre en la rama que itera `geometry.groups`, exclusiva de un material array). Con el material único que `buildCuboidMesh` usaba desde el ticket 008, `pickCuboidFaceAt` habría devuelto SIEMPRE la primera cara (`east`), sin relación con el clic real. Corregido cambiando `mesh.material` a un array de 6 slots con la MISMA instancia repetida (cero cambio de render -- cada grupo se sigue pintando con el mismo material de siempre) -- rompe cualquier código/test que asumiera `mesh.material` como objeto único (actualizados: `buildMobScene.spec.ts`, `ThreeViewportService.spec.ts`, `TextureCanvas.spec.ts`).
  - **Discrepancia real encontrada contra el Diseño técnico §14 (reportada, no parcheada en silencio)**: el documento describe la validación dev-only de la normal como "la normal esperada del `FaceName` resuelto, **compuesta con la rotación del mesh**". Verificado contra el código fuente real de Three.js (`checkGeometryIntersection`): `intersection.face.normal` se calcula con `Triangle.getNormal` sobre vértices en ESPACIO LOCAL del mesh (el rayo se transforma a local antes del test, nunca al revés) -- es decir, la normal reportada NUNCA incorpora la rotación mundial del mesh. Componer la rotación como describe el documento habría producido falsos positivos de "discrepancia" para cualquier cuboid rotado (justo el caso que el propio AC exige cubrir: "incluyendo uno rotado"). Implementado según el comportamiento REAL verificado (comparación directa contra `FACE_LOCAL_NORMALS`, sin componer rotación) -- ver test dedicado que rota un cuboid 90°/45° y confirma CERO falso positivo, más el test que fuerza una discrepancia real (`materialIndex` resuelve `up`, normal reportada es la de `east`) y confirma que el resultado de `pickCuboidFaceAt` no cambia.
  - `textureSelectionStore.ts` (nuevo, `frontend/src/editor/texture/`): store Pinia separado de `selectionStore.ts` (017/031/036, selección de CUBOID completo) -- estado único `selectedFace: {cuboidId, face} | null`. Verificado con test cruzado explícito (`textureSelectionStore.crossStoreIndependence.spec.ts`, mismo patrón que 046) que ninguno de los dos store se filtra al otro y que `selectionStore.ts` no ganó ningún campo nuevo.
  - **Selección cruzada bidireccional en `TextureCanvas.vue`**: `selectedRegionKey` (ref local desde 047) pasa a ser un `computed` de lectura/escritura sobre `textureSelectionStore.selectedFace` -- el `<select>`/overlay SVG existentes no cambian de template, ahora leen del store compartido. Un clic en el preview 3D (mismo criterio click-vs-drag de umbral que `ThreeViewport.vue`/017) llama `pickCuboidFaceAt` y escribe al store; cualquier cambio del store (clic 3D o dropdown 2D) reconstruye el preview vía `buildMobGroup`'s nuevo 4to parámetro `selectedFace`, que agrega un highlight (`LineLoop` sobre el perímetro real de esa cara, mismo color que el outline de cuboid completo) como hijo del mesh -- deliberadamente separado del `else` (needsUpdate-only) de `syncDataTexture` para no reconstruir todos los meshes en cada trazo de pintado (la optimización de 047 sigue intacta).
  - **Sin cambios de contrato**: `selectionStore.ts` (017/031/036) no se tocó -- sus tests existentes pasan sin modificación. `buildMobGroup`/`ThreeViewportService.setModel` ganan un 4to parámetro opcional (`selectedFace`); los callers que no lo pasan (`ThreeViewport.vue`/`GenerationPreviewViewport.vue`, tab Modelo) no cambian de comportamiento.
  - Frontend: 463 tests, 0 failures (64 archivos, +22 nuevos sobre la base de 047 -- `buildMobScene.spec.ts`, `ThreeViewportService.spec.ts`, `TextureCanvas.spec.ts` ganan tests nuevos; `textureSelectionStore.spec.ts`/`textureSelectionStore.crossStoreIndependence.spec.ts` son archivos nuevos). `vue-tsc -b` y `eslint --max-warnings 0` en verde.
- **Import de PNG -- región seleccionada y atlas completo, crop/pad nunca escalado** (`done/048-...`, Diseño técnico §8, HU-28, frontend): nuevo `textureImportTools.ts` (funciones puras, sin canvas/DOM, mismo criterio de testabilidad que `pixelTools.ts` de 047) -- `cropOrPad(source, targetWidth, targetHeight)` copia byte a byte el área superpuesta con el destino, alineada a la esquina superior izquierda (decisión de implementación documentada explícitamente, el ticket no fijaba la alineación): recorta el excedente si el origen es más grande en un eje, rellena TRANSPARENTE si es más chico -- nunca resamplea ni un solo píxel, por construcción no existe ninguna ruta de escalado en el módulo (verificado con un test que enumera sus exports). `describeImportAdjustment` compara dimensiones origen/destino y devuelve qué crop/pad aplicaría, sin tocar píxeles. Nuevo `pngImportDecode.ts` (`decodePngFileToAtlasBuffer`, aislado del resto por ser el único punto que depende de `createImageBitmap`+canvas 2D real -- ninguno de los dos existe en jsdom, mismo gotcha ya documentado por `pixelTools.ts`/047) decodifica el `File` elegido por el usuario a un `AtlasBuffer` RGBA 1:1, sin escalar, liberando siempre el `ImageBitmap` (`close()` en `finally`).
  - **`TextureImportPanel.vue`** (nuevo componente, montado por `TextureCanvas.vue`): reutiliza el selector de región de 047 como destino del import -- "Todas las caras" = atlas completo (AC B), una región puntual = esa región (AC A) -- en vez de agregar un control redundante. Mismo flujo para ambos casos: elegir archivo -> decodificar -> SIEMPRE mostrar un panel de confirmación (mismo patrón visual de toggle Antes/Después de un solo preview que `AiEditPanel.vue`, HU-17/18) -- sin confirmar explícitamente (Cancelar o no interactuar) no se aplica nada -- al confirmar, EXACTAMENTE una llamada a `textureEditorStore.recordPatch(rect, before, after)`, un único `TexturePatchCommand`. El destino real (`rect`/`label`) se resuelve mediante una función (`resolveTarget`) pasada como prop, evaluada recién en el momento de procesar el archivo elegido, en vez de viajar como prop de valor estático -- evita una condición de carrera real encontrada en desarrollo (ver hallazgo abajo).
  - **Estado B (atlas congelado, 042) satisfecho por construcción**: el destino del import siempre se deriva del atlas/región YA vigentes (`textureEditorStore.atlas`, cargado con las dimensiones reales del modelo) -- este componente jamás deriva un tamaño nuevo desde el PNG importado, así que "crop/pad hacia las dimensiones congeladas, nunca las cambia" no requirió ninguna rama de código especial para una región `PAINTED`, solo un test que lo verifica explícitamente.
  - **Hallazgo real, corregido durante la implementación**: el primer diseño pasaba el destino (`targetRect`) como prop de VALOR, calculado en `TextureCanvas.vue` a partir de `textureEditorStore.atlas` -- pero ese atlas se carga recién en el `onMounted` del padre, que corre DESPUÉS del primer montaje de sus hijos; el recómputo resultante de props (`0×0` -> dimensiones reales) se aplica en un patch de Vue diferido (microtask), no de forma síncrona. Un test que dispara el import inmediatamente después de montar (sin esperar un tick) veía todavía el snapshot `0×0` del primer render, produciendo un `TexturePatchCommand` con `rect` vacío. Corregido pasando el destino como una FUNCIÓN (`resolveTarget: () => { rect, label }`) evaluada en el momento exacto de procesar el archivo -- lee el estado reactivo vigente sin depender de que Vue ya haya "aplicado" el re-render del padre. Mejora continua propuesta al Product Owner: este es el mismo tipo de gotcha de timing de reactividad de Vue que 047 ya documentó para `triggerRef`/`watch` -- candidato a agregarse a la memoria de gotchas del equipo para que futuros componentes que deriven props de un store cargado en `onMounted` lo eviten desde el diseño.
  - **AC "nunca escalar" verificado en dos capas**: `textureImportTools.spec.ts` verifica que el módulo no exporta ninguna función de nombre relacionado a escalar/resize; `TextureImportPanel.spec.ts` verifica que ningún botón/control del panel de confirmación (en los tres casos: crop, pad, exacto) ofrece esa opción, y que no existe ningún `<select>`/`<input type=range>` de "factor de escala". Sin cambios de backend: `TextureService.upload` (045) ya no realiza ningún resize/scale sobre los bytes recibidos (verificado leyendo su código) -- el import completo ocurre en memoria del lado del cliente, mismo mecanismo que Pincel/Borrador/Cubeta (047), antes de que el atlas resultante se persista por el flujo ya existente.
  - **Fuera de alcance, sin cambios (explícito en el ticket)**: exportar la textura como PNG independiente.
  - **Revisión visual en vivo no realizada**: mismo gap ya señalado en 043/047 -- sin acceso a Claude in Chrome en este entorno de implementación (worktree aislado); verificado solo con tests automatizados y el chequeo estático de accesibilidad. Pendiente antes de dar la UI del panel de import por validada al 100%.
  - Frontend: 469 tests, 0 failures (65 archivos, +28 nuevos: `textureImportTools.spec.ts` 10, `pngImportDecode.spec.ts` 4, `TextureImportPanel.spec.ts` 9, +5 en `TextureCanvas.spec.ts`). `vue-tsc -b` y `eslint --max-warnings 0` sin hallazgos.

- **Pipeline completo de generación de textura por IA: SSE + diff + Apply atómico + 409** (`done/054-...`, HU-36 a HU-39, Diseño técnico §6/§10/§11/§13/§16/§21): `TextureGenerationService` (nuevo, `aiorchestrator/texture/`) orquesta de punta a punta lo que 052/053 dejaron listo como piezas sueltas -- mismo patrón de orquestación que `MobGenerationService` (028/029): `startGeneration` es síncrono hasta crear la fila `ai_jobs` (`job_type=generate_texture` si `boneId` es `null`/ausente -- HU-36, todos los bones con geometría; `job_type=edit_texture` si viene poblado -- HU-37, un bone puntual), el pipeline real corre en `generationExecutor` reutilizado tal cual. Por cada bone objetivo: `TexturePlanService.analyze` (052, UNA vez) → `TextureGenerationSheetPlanner.plan` (053, por bone) → por cada sheet/parte (fallback de batching incluido): `ImageGenerationProvider.generateTextureSheet` (051) → `TextureSheetSlicer.slice` + `TextureCompositorService.compose` (053) sobre una copia en memoria del atlas, acumulada across bones. Nuevos `GenerationStage` reutilizando `ai_job_events`/SSE tal cual (029): `analizando_paleta`, `mapeando_caras`, `generando_bone_<id>` (único valor DINÁMICO de toda la clase -- incluye el id real del bone, con sufijo `"(parte N/M)"` en el `message` si hubo batching), `componiendo_atlas` (payload `preview_texture_patch`), `limpiando_pixeles` (stage emitido, sin transformación real -- ver "simplificación documentada" abajo). `TextureGenerationController` (nuevo) expone `POST /api/mobs/{mobId}/ai/generate-texture`, `GET /api/jobs/{jobId}/texture-result`, `POST /api/jobs/{jobId}/apply-texture` -- `GET /api/jobs/{jobId}/events` (029) se reutiliza SIN tocarlo, es genérico por `jobId`.
  - **Esquema formal de `preview_texture_patch` (Diseño técnico §13)**: `{type, rect:{x,y,width,height}, encoding:"base64"|"asset_url", data|url}` -- umbral de 32 KB del payload base64: por debajo, inline; por encima, sube a MinIO bajo `texture-previews/{jobId}/{seq}.png` (prefijo DISTINTO de `textures/`, nuevo `TexturePreviewAssetController` lo sirve por `GET /api/texture-previews/{jobId}/{fileName}`) y emite `asset_url`. Verificado con un test AISLADO (`TextureGenerationServicePreviewPatchTest`, sin Testcontainers) que fuerza el cruce del umbral con ruido aleatorio real (incompresible) -- el PNG sintético determinista de `MockImageProvider` (tablero de ajedrez) es demasiado compresible para cruzarlo nunca a través del pipeline completo, sin importar el tamaño pedido.
  - **Ningún preview toca `mob_drafts`/`mob_revisions`/`textures/{hash}.png` fuera de un Apply real** -- verificado con un test explícito (`TextureGenerationServiceTest`) que corre el pipeline completo y confirma `draft_version`/conteo de `mob_revisions`/`current_revision_number` sin cambios, y que el SHA-256 del atlas compuesto NUNCA aparece como key existente en MinIO hasta que se llama `apply-texture`.
  - **Apply atómico (Diseño técnico §10), reutilizando `DraftPersistenceService.applyGenerationProposal` (030) TAL CUAL**: (1) el bitmap compuesto se sube a MinIO (`TextureService.upload`, 045 -- backend recalcula el hash real recién acá) PRIMERO, fuera de la transacción; (2) `applyGenerationProposal` corre como UNA transacción Postgres que actualiza `mob_drafts` + inserta `mob_revisions` (texture+geometría juntas) + avanza `current_revision_number`. **Hallazgo real, corrige una afirmación del propio documento de definición**: el Diseño técnico §10 describe esto como una "divergencia deliberada" porque asume que el Apply de geometría ya existente "SOLO actualiza el draft" -- verificado contra el código real (`AiEditService.applyEdit`/`GenerationResultService.apply`, ambos delegan en este mismo método desde el ticket 030): `applyGenerationProposal` YA hacía las 3 escrituras atómicamente desde 030, antes de este ticket. No hizo falta ningún cambio de comportamiento -- se reutiliza sin modificar, y este hallazgo se reporta para que el documento de definición no quede con una premisa desactualizada. Verificado con un test de integración dedicado (`@MockitoSpyBean` sobre `MobRepository`, fuerza un `RuntimeException` en el último `save()` de la transacción) que confirma: ninguna fila de `mob_revisions`/`mob_drafts`/`mobs.current_revision_number` queda escrita a medias.
  - **Conflicto 409 (`STALE_TEXTURE_BASE`, Diseño técnico §16)**: mismo mecanismo de `base_revision_number`/`base_draft_version` ya usado por `StaleEditBaseException` (031) -- CUALQUIER avance del draft/revisión compartido (geometría O textura, ambas viven en el mismo `MobProjectModel`) invalida el Apply, verificado simulando un avance de `draft_version` independiente.
  - **Migración `V3__ai_jobs_texture_job_types.sql`**: `ai_jobs.job_type` acepta `generate_texture`/`edit_texture`; nueva columna `target_bone_id` (nullable). **Hallazgo real, más allá de lo que el ticket pedía literalmente**: `chk_ai_jobs_base_values_by_type` (003) también tuvo que actualizarse -- a diferencia de `generate` (arranca sin geometría), `generate_texture`/`edit_texture` SIEMPRE corren sobre un mob con `revision_number >= 1`, así que necesitan `base_revision_number`/`base_draft_version` `NOT NULL` (mismo criterio que `edit`) para que el chequeo 409 tenga contra qué comparar -- sin esta corrección, todo insert válido de estos 2 job types nuevos habría violado la constraint.
  - **`ImageGenerationProvider` (051) gana `provider()`/`model()` de forma aditiva**: `generateTextureSheet` devuelve solo `byte[]` (a diferencia de `VisionModelProvider`/`StructuredReasoningProvider`, que ya empaquetan un `AiProviderResponse` completo) -- sin esto, `TextureGenerationService` no tendría forma de saber qué proveedor/modelo REAL persistir en `ai_jobs` (HU-39) sin un `instanceof` frágil. `OpenAiImageProvider.model()` (ya existía desde 051) sube a la interfaz; `provider()` es nuevo en ambos (`"openai"`/`"mock"`).
  - **`UvRegion` gana `paintedBy` (`UvPaintOrigin`: `hand`/`ai`), aditivo -- gap real señalado explícitamente**: ni el Diseño técnico ni el ticket 054 fijaban CÓMO distinguir "contenido pintado a mano" de "generado por IA" para HU-37 AC #2 (`PAINTED`, desde el ticket 040, no lleva información de origen). Se agrega este campo con el mismo mecanismo exacto que `status` (040): constructor de conveniencia de 4 argumentos preservado (`paintedBy=null`, cero cambio de forma para ningún call site existente), `@JsonInclude(NON_NULL)` para no romper comparaciones JSON estrictas ya congeladas. Convención de default deliberadamente conservadora: `null`/ausente (TODO JSON legacy, y toda región pintada a mano por el editor manual del ticket 047, que todavía no escribe este campo explícitamente) se trata como "origen desconocido, posible pintado a mano" -- nunca se asume `ai` por default, nunca se subestima el riesgo de sobrescribir trabajo real del usuario. Contratos espejo actualizados de forma aditiva (`contracts/schemas/mob-project-model.schema.json`, `frontend/src/domain/MobProjectModel.ts`), mismo mecanismo ya usado para `status`. **Gap explícito para un ticket futuro**: el editor de pintado manual (047) no escribe `paintedBy=hand` todavía -- el default conservador ya cubre la corrección de HU-37 AC #2 sin eso, pero la auditoría quedaría más completa si lo hiciera.
  - **Simplificación documentada, sin over-engineering**: la etapa `limpiando_pixeles` se emite (satisface el AC de que la SSE exponga ese valor de `stage`) pero SIN una transformación de post-procesado real -- el aislamiento espacial de 053 (clipping exacto por `atlasUvRect`, sin bleed posible) ya deja el atlas compuesto sin nada que "limpiar" que ningún AC concreto de este ticket exija verificar; inventar un algoritmo de limpieza de paleta no pedido por ningún AC habría sido alcance no solicitado.
  - **Fuera de alcance de este ticket, explícito**: las pantallas 07/08 (mockups, tickets 050/055) y el wiring frontend del selector de estilo/detalle/bone -- este ticket es 100% backend (pipeline + API), consistente con el alcance real fijado por el ticket.
  - Backend: 401 tests, 0 failures (+32 nuevos: `SchemaConstraintsTest` +5, `MockImageProviderTest` +1, `UvRegionUvReservationContractTest` +3, `TextureGenerationServicePreviewPatchTest` 3, `TextureGenerationServiceTest` 7, `TextureGenerationApplyTest` 4, `TextureGenerationControllerTest` 7, `TexturePreviewAssetControllerTest` 2).
- **Pantalla del editor de textura -- el tab "Textura" pasa de deshabilitado a funcional** (`done/050-...`, HU-41, mockup 07, frontend): `EditorHeader.vue` (036) deja de fijar `model-value="modelo"` a fuego -- gana `activeTab`/`update:activeTab` (mismo contrato v-model que ya exponía `GTabs.vue`, sin tocar ese componente) y la tab "Textura" pierde `disabled`/`disabledReason` (ya no es "Próximamente"); "Animación" sigue exactamente igual (deshabilitada, Fase 4). `MobEditor.vue` gana `activeTab` (ref local, `'modelo'` por default) y decide qué monta el body: en `'textura'`, monta EXCLUSIVAMENTE `TextureCanvas.vue` (047/048/049, ya completo -- este ticket lo ENSAMBLA, no lo reimplementa) con el MISMO `draft.model` ya cargado arriba (sin segundo fetch ni estado duplicado); `EditorToolbar`/`HierarchyPanel`/`InspectorPanel`/`AiEditPanel` (geometría/asistente IA de la tab Modelo) y la fila de acciones superior (Reset cámara/Asistente IA/Exportar, no presentes en el mockup 07) se ocultan en esa tab -- fidelidad estricta a la estructura del mockup, sin inventar controles nuevos ni tocar el layout interno de 3 columnas que `TextureCanvas.vue` ya resolvía por su cuenta desde 047.
  - **Decisión de alcance, consistente con el mockup (no una improvisación)**: el mockup 07 solo muestra breadcrumb+tabs en el header, sin la fila de botones que sí tiene la tab Modelo -- se opta por ocultar esa fila en vez de mostrarla igual "por consistencia de chrome", precisamente para no reinterpretar la estructura del mockup sin VoBo (regla explícita del ticket). El `GSidebar` de navegación global sí se mantiene en ambas tabs -- es chrome de toda la app (`ProjectDetail`/`ProjectsDashboard`/`AiMobWizard`/`ExportScreen` lo usan igual), no algo específico de la pantalla Modelo que el mockup 07 recorte a propósito.
  - **Singleton de canvas WebGL, mismo patrón ya probado**: `TextureCanvas.vue` adjunta/libera el mismo `ThreeViewportService` singleton a su propio contenedor de preview en `onMounted`/`onBeforeUnmount` -- idéntico mecanismo que `ThreeViewport.vue`/`GenerationPreviewViewport.vue` ya usaban para coexistir vía `v-if` (031); cambiar de tab Modelo<->Textura reclama/libera el canvas exactamente igual que alternar Editor manual<->Asistente IA ya hacía, sin código nuevo de arbitraje.
  - **Fuera de alcance de este ticket, explícito**: HU-24 completa (etiquetado de región/acceso sin geometría todavía) ya estaba resuelta por 047/034 -- este ticket no repite esa lógica, solo la deja alcanzable desde el tab real. Guardar unificado de texture+geometría (HU-30/31) y el generador de textura por IA (mockup 08, ticket 055) no se tocan.
  - **Revisión visual en vivo pendiente**: mismo gap ya señalado en 043/047/048 -- `list_connected_browsers` devolvió vacío en este worktree aislado, verificado en la misma sesión. Queda pendiente que la sesión principal (con su propio acceso a Chrome) contraste la pantalla corriendo contra el mockup 07 antes de dar el ticket por completamente cerrado.
  - Frontend: 498 tests, 0 failures (68 archivos, +7 nuevos sobre la base previa: `EditorHeader.spec.ts` 4 tests, archivo nuevo; +3 en `MobEditor.spec.ts`). `vue-tsc -b` y `eslint --max-warnings 0` sin hallazgos.
- **`TextureAiGeneratorScreen.vue` -- pantalla del generador de textura por IA, mockup 08** (`done/055-...`, HU-36/HU-37/HU-38/HU-42, frontend): nueva ruta propia `/projects/:projectId/mobs/:mobId/texture/generate-ai` que ensambla el pipeline 100% backend de 054 en una pantalla real, siguiendo el MISMO patrón ya probado por el wizard de geometría (`GenerationStep.vue`/`AiEditPanel.vue`, 029/031/038) en vez de inventar uno nuevo: configurar (Estilo -- 4 opciones, Detalle bajo-alto, "Parte a generar" con granularidad de bone completo) -> progreso SSE con preview incremental -> diff Antes/Después con Apply/Reject. El panel "resultado/preview" del mockup 08 es el ATLAS 2D (no el viewport 3D, a diferencia del wizard de geometría) -- coherente con que 054 opera sobre píxeles del atlas: `preview_texture_patch` (`analizando_paleta`/`mapeando_caras`/`generando_bone_<id>`/`componiendo_atlas`/`limpiando_pixeles`, nuevo `frontend/src/ai/texture/textureGenerationStages.ts` normaliza el stage dinámico `generando_bone_<id>` a una categoría fija para la lista de etapas) se compone incrementalmente sobre un `<canvas>` del tamaño real del atlas vía `decodeTexturePreviewPatch` (nuevo, `texturePatchDecode.ts` -- mismo criterio de aislamiento de `createImageBitmap`/canvas real que `pngImportDecode.ts`, 048, para mantenerlo testeable sin DOM real). Al completar, el diff Antes/Después reemplaza el canvas por las 2 imágenes completas que ya devuelve `GET /api/jobs/{jobId}/texture-result` (`beforeAtlasPngBase64`/`afterAtlasPngBase64`) con el mismo toggle Antes/Después de `AiEditPanel.vue`; "Rechazar" nunca llama a ningún endpoint (HU-38: "Reject no modifica ni el draft ni ninguna revisión" -- no existe un endpoint de reject, ver `docs/API.md`); "Aplicar" llama `POST /apply-texture`, traduce un 409 `STALE_TEXTURE_BASE` a un aviso explícito con "Regenerar contra el estado actual" (mismo patrón que `STALE_EDIT_BASE`/031) y navega de vuelta al editor del mob en éxito. Nuevos clientes HTTP `frontend/src/api/textureGenerationApi.ts` (start/result/apply) y `listReferenceImages`/`referenceImageUrl` agregados a `referenceImagesApi.ts` (primer consumidor real de `GET /api/mobs/{mobId}/references`, 024, backend-only hasta ahora) para mostrar la MISMA imagen de referencia ya subida en Fase 2 (HU-36 AC #1) sin pedir una nueva.
  - **Integración con el tab "Textura" -- gap documentado a propósito, no una omisión**: este ticket depende solo de 054 (backend), NO de 050 (todavía `pending` -- hace funcional el tab "Textura" completo ensamblando 047/048/049 dentro de `MobEditor.vue`/`EditorHeader.vue`, mockup 07). Cablear el botón real "Generar con IA" DENTRO de ese tab queda para 050 cuando aterrice -- mismo criterio que `AiMobWizard.vue` es una ruta propia en vez de vivir dentro de `MobEditor.vue`. El botón "×" de esta pantalla ya vuelve al editor real del mob (`/edit`), lista para que 050 agregue un link real hacia esta ruta sin tocar nada de este ticket.
  - **Revisión visual en vivo no realizada**: mismo gap ya señalado en 043/047/048 -- `list_connected_browsers` devolvió vacío en este worktree aislado (verificado en la misma sesión), sin acceso a Claude in Chrome. Verificado solo con tests automatizados y el chequeo estático de accesibilidad de cada commit; pendiente una pasada visual contra el mockup 08 antes de dar la UI por validada al 100%.
  - Frontend: 521 tests, 0 failures (71 archivos, +30 nuevos sobre la base de 491/67: `TextureAiGeneratorScreen.spec.ts` 11, `textureGenerationStages.spec.ts` 7, `texturePatchDecode.spec.ts` 4, `textureGenerationApi.spec.ts` 5, +3 en `referenceImagesApi.spec.ts`). `vue-tsc -b` y `eslint --max-warnings 0` sin hallazgos.
- **Suite E2E de aceptación de Fase 3 -- y cierre de DOS gaps reales de AC#1/#2, ampliación de alcance decidida explícitamente por el PO en checkpoint** (`done/056-...`, HU-43, último ticket de Fase 3, 17/17): al preparar la suite se confirmó que el AC#1 del ticket ("pintar a mano + Guardar crea una `mob_revision` con textura real") NO era satisfacible -- `textureEditorStore.ts`/`TextureCanvas.vue` (046/047) nunca se conectaban a `draftModelStore.ts` ni al backend, solo el camino de generación por IA (054, vía "Aplicar") persistía textura real. El PO decidió explícitamente cerrar el gap dentro de este mismo ticket, en vez de dejarlo como hallazgo sin resolver:
  - **Cliente HTTP + flush de "Guardar" (AC#1)**: nuevo `frontend/src/api/textureUploadApi.ts` (`PUT /api/mobs/{mobId}/texture`, 045, primer consumidor frontend) + `frontend/src/editor/texture/textureAtlasEncode.ts` (atlas RGBA en memoria -> `Blob` PNG real, dirección inversa de `pngImportDecode.ts`, 048) + `frontend/src/editor/texture/textureFlush.ts` -- módulo puente NUEVO que conecta `textureEditorStore` con `draftModelStore` SIN acoplarlos entre sí (ninguno de los dos importa al otro; `textureEditorStore.ts` conserva intacta su garantía de independencia, 046). `EditorToolbar.handleSave` ahora hace `await flushPaintedTexture(model)` ANTES de `saveRevision` -- sube el atlas vigente y actualiza `texture.storageKey` al valor OFICIAL del backend antes de crear la Revision (mismo criterio "flush obligatorio" que HU-30/HU-31, ya defendido del lado backend desde 045); si no hay ningún atlas cargado (tab Textura nunca abierto) es un no-op transparente, y si el flush falla el Guardar completo se aborta (nunca una Revision con `storageKey` colgante).
  - **Segundo gap real, encontrado DESPUÉS de cerrar el primero, mismo checkpoint del PO (AC#2)**: `BBModelExporterV5`/`V4` nunca leían `model.texture().storageKey()` -- TODO export embebía siempre el checkerboard placeholder (011), incluso con textura real ya persistida. El PO aprobó explícitamente cerrarlo también, con el enfoque propuesto: el exportador SIGUE siendo un serializador 100% puro (respeta el "Hallazgo A revertido" de 041/044) -- nuevo overload `export(model, byte[] realTexturePngBytes)` (`null` preserva EXACTAMENTE el comportamiento de siempre); `MobExportService` (que ya tiene acceso a los repos) resuelve los bytes reales vía `AssetStorageService.get(storageKey)` y se los pasa, mismo patrón que `LegacyUvNormalizationService` (044) -- resolución explícita del CALLER, nunca dentro del exportador. Si el `storageKey` ya no existe en el storage (invariante roto en teoría, defendido en profundidad desde 045), cae al placeholder con un `WARN` explícito en vez de romper el export. Tests nuevos: `BBModelExporterV5RealTextureTest`/`BBModelExporterV4RealTextureTest` (unitarios) + un caso end-to-end en `MobExportControllerTest` (sube textura real -> guarda Revision -> exporta -> bytes reales embebidos, `fmmCompatible` sigue en verde).
  - **Suite Playwright nueva** (`frontend/e2e/fase3-textura-acceptance.spec.ts`, mismo patrón que `technical-alpha-acceptance.spec.ts`/033): mob con geometría ya usable (revisión ≥ 1, vía "Usar este modelo") -> pintar textura A MANO en el tab Textura (el camino recién cerrado arriba, deliberado -- el camino IA ya tiene cobertura propia) -> Guardar -> exportar `.bbmodel` -> confirma `textures[0].name === "texture"` (nunca `"placeholder"`) con un PNG real decodificable -> panel FMM (`Modelo listo para usar en tu servidor`, HU-20) en verde.
  - **3 bugs reales adicionales, encontrados al correr la suite completa (033+056) de punta a punta de verdad -- ninguno relacionado con el gap ya decidido, corregidos como mantenimiento necesario para un E2E genuinamente verde (nunca detectados antes porque esta suite no corre en CI, mismo gap del 033, y nadie la había re-corrido localmente tras los tickets que la rompieron en silencio)**: (1) `GenerationEventBroadcaster.sendTo` solo capturaba `IOException` -- una generación mock (casi instantánea) puede completar el job en OTRO hilo exactamente mientras `replay()`/`publish()` todavía iteran sobre el mismo emitter; `SseEmitter.send()` en ese caso lanza `IllegalStateException` (no `IOException`), que tumbaba el endpoint SSE completo con un 500 (el `EventSource` del navegador quedaba "Reconectando al proceso..." para siempre) -- ahora se trata igual que un cliente desconectado, test nuevo `GenerationEventBroadcasterTest`. (2) `WebConfig` (CORS) nunca incluía `PUT` en `allowedMethods` -- gap real desde el 045, nunca ejercitado desde un navegador real hasta que este ticket conectó el primer consumidor frontend de `PUT /texture`. (3) `MockReasoningProvider.defaultEditResponseFor` (rama "modelo real", 031) reescalaba el primer cuboid a 1.2 -- combinado con el packing real de densidad de texel (042, posterior a 031), desborda el atlas por defecto con `UV_ATLAS_OVERFLOW` real; reducido a 1.05 (sigue siendo un resize real y visible), explícitamente test/dev-only (`AI_REASONING_PROVIDER=mock`), cero impacto en Claude real. `technical-alpha-acceptance.spec.ts` (033) también tenía varios selectores desactualizados por pasadas de UX posteriores (039/050) -- corregidos en el mismo ticket para que la suite completa, no solo la mitad nueva, corra de verdad.
  - **Mismo gap de CI conocido que 033, mismo criterio ya aceptado por el PO**: la suite sigue sin correr en el `Jenkinsfile` compartido (Docker-outside-of-Docker/CORS entre contenedores hermanos) -- no se reabrió el diagnóstico, se documenta y se hereda la decisión ya tomada en 033. `scripts/e2e-up.sh` gana `AI_IMAGE_PROVIDER=mock` (antes solo vision/reasoning) para que cualquier escenario que ejercite el pipeline de textura por IA use `MockImageProvider`, nunca OpenAI real.
  - **Verificación real, no asumida**: corrida localmente contra un stack aislado equivalente al de `scripts/e2e.sh` (Postgres+MinIO+backend+frontend+Chromium reales, en puertos alternos porque el checkout compartido tenía su propio stack corriendo en los puertos fijos de siempre en esta sesión) -- 2/2 specs en verde, repetido 3 veces consecutivas (secuencial y en paralelo). Backend completo: 409/409 tests. Frontend completo: 539/539 tests, lint y typecheck limpios.
  - **Fase 3 cerrada** (17/17 tickets, 040-056) -- editor de textura/UV manual + generación de textura por IA + persistencia/exportación de textura real, de punta a punta, con su propia suite de aceptación E2E (mismo criterio de cierre que el Technical Alpha/Fase 1+2, ticket 033).

**Addendum post-milestone -- corrección de `UvLayoutSelector`** (`done/057-...`, HU-34, NO es un ticket del milestone original de Fase 3, nace de una revisión posterior del PO sobre el trabajo ya entregado -- ver `docs/definiciones/galgoth-studio-fase3-textura.md`, addendum del 10 sep 2026):
- **Hallazgo real, confirmado en el código ya en `dev`**: `UvLayoutSelector.resolve()` (041) decidía la estrategia mirando ÚNICAMENTE `previousLayout.regions()` (`PAINTED`/`ORPHAN`), ignorando por completo `previousLayout.reservations()`. Un resize confirmado sobre la ÚNICA región `PAINTED` de un layout la reempaqueta como `UNPAINTED` y crea una `UvReservation` para el rect abandonado (`StableUvStrategy`, 041) -- el layout resultante quedaba con `PAINTED=0`/`ORPHAN=0`/`reservations>0`, y la regla anterior elegía `AlphaAutoPackStrategy` para el siguiente `Add`, que no conoce las reservas y podía reempaquetar libremente ENCIMA del rect reservado -- exactamente lo que `UvReservation` existe para impedir.
- **Corrección**: regla encapsulada en `UvLayoutSelector.requiresStableLayout(UvLayout)` -- `StableUvStrategy` si `PAINTED>0` O `ORPHAN>0` O `reservations>0`; `AlphaAutoPackStrategy` solo si las tres son 0 (comportamiento de Fase 1+2 sin cambios en ese caso). Javadoc de la clase actualizado con el hallazgo.
- **TDD real**: 3 tests nuevos en `UvLayoutSelectorTest` escritos ANTES del fix (A y C fallaban contra el código viejo, confirmado localmente) -- (A) `PAINTED=0`/`ORPHAN=0`/`reservations=1` -> `StableUvStrategy`; (B) todo `UNPAINTED`/`reservations=0` -> `AlphaAutoPackStrategy` (sin cambios); (C) secuencia completa end-to-end sobre el selector real -- resize confirmado de la única región `PAINTED` crea la reserva, y el `Add` siguiente sobre ese layout nunca ocupa el rect reservado.
- **Verificación real**: `./gradlew clean test` completo, backend: 412/412 tests, 0 failures.

**Addendum post-milestone -- rediseño de la pantalla del editor de textura (mockup 07 v2)** (`done/058-...`, HU-27/HU-41, NO es un ticket del desglose original de Fase 3, nace de una revisión visual directa del PO sobre el trabajo ya entregado en 050 -- flujo preview-first ya establecido del proyecto, VoBo explícito y dedicado tras 2 rondas de ajustes sobre un Artifact interactivo validado, ver `docs/definiciones/mockups/058-texture-editor-redesign-reference.html`):
- **Motivo**: el layout de 050 (columna lateral izquierda de herramientas) comprimía el lienzo -- que debería ser el foco visual dominante de la pantalla -- y no respetaba la línea visual horizontal del resto del producto. Cambio 100% de shell/UX: ningún mecanismo de dominio (`pixelTools.ts`/`textureEditorStore.ts`/`textureSelectionStore.ts`/`ThreeViewportService.ts`) se tocó.
- **Layout en 3 zonas** (`frontend/src/editor/texture/TextureCanvas.vue`, reescrito): toolbar horizontal arriba (región, herramientas de pintado, grid, tamaño de pincel, color activo, zoom, Importar PNG, Generar con IA, estado de guardado, Guardar) + lienzo UV/atlas dominante al centro + preview 3D secundario (más angosto) a la derecha + barra de estado inferior opcional (región | zoom | herramienta | guardado).
- **Zoom/pan reales sobre el lienzo, nuevos en este ticket**: `zoomPercent` (25%-1600%) vía botones +/-/reset, dropdown de presets (50/100/200/400/800/1600%), atajos de teclado (`+`/`-`/`0`) y `Ctrl`+rueda centrado en el cursor (convierte el punto bajo el cursor a coordenadas de atlas antes de escalar, ajusta el scroll después para que quede fijo). Pixel-perfect en todo el rango (`image-rendering: pixelated`, resolución intrínseca del canvas siempre igual a la del atlas -- el zoom solo cambia el tamaño CSS del stage). Pan con barra espaciadora + arrastre, y scroll nativo (`.app-scroll`, ya estilizado desde el ticket 039). `canvasPointToAtlas` (cálculo de ratio canvas.width/rect.width) no cambió una línea -- es zoom-agnóstico por construcción.
- **`GSelect.vue` nuevo en el design system** (`frontend/src/design-system/components/GSelect.vue`): primer componente de select-con-valor genérico del proyecto (value binding, navegación por teclado ArrowUp/ArrowDown/Enter/Escape, opción activa en `--accent`, mismo patrón ARIA sin `<ul>`/`<li>` ya establecido por `GMenu.vue`). Reemplaza los `<select>` nativos de región/tamaño de pincel de 047 y es la base del nuevo selector de presets de zoom -- AC verificado con un test dedicado que falla si aparece un `<select>` nativo en el DOM de la pantalla.
- **`TextureColorPicker.vue` nuevo** (`frontend/src/editor/texture/`): trigger + panel con la paleta fija de 8 colores (ahora en `texturePalette.ts`, extraída de `TextureCanvas.vue` para compartirla) + swatch "personalizado" que dispara un `input[type=color]` nativo oculto (única excepción explícita a "sin controles nativos", ya aceptada por el PO) + campo de texto hex -- así se puede elegir CUALQUIER color, no solo los 8 fijos.
- **`TextureSaveStatus.vue` nuevo**: indicador de 4 estados (guardado/cambios sin guardar/guardando/error), reutilizando EXACTAMENTE el mecanismo de flush del ticket 056 (`textureFlush.ts`/`textureAtlasEncode.ts`/`textureUploadApi.ts`) + `draftPersistenceApi.saveRevision` -- antes el tab Textura no tenía ningún botón "Guardar" propio (había que volver al tab Modelo). La orquestación (`handleSave` dentro de `TextureCanvas.vue`) se duplica deliberadamente respecto a `EditorToolbar.handleSave` en vez de extraerse a un módulo compartido -- ambos llaman a los mismos módulos de fondo, sin lógica de negocio duplicada, y `EditorToolbar.spec.ts` (ajeno a este ticket) queda intacto.
- **Cuentagotas con confirmación visible**: un toast temporal (`role="status" aria-live="polite"`) muestra el hex capturado -- antes era un cambio silencioso (solo el swatch, sin ningún otro indicio).
- **Preview 3D orbitable -- sin cambios en `ThreeViewportService.ts`**: `OrbitControls` ya estaba habilitado por defecto para este contexto (solo se deshabilita durante el arrastre de `TransformControls`, que no aplica a la tab Textura) -- se verificó que el arrastre para orbitar ya funcionaba de fábrica, coexistiendo con la selección cruzada cuboid-UV del ticket 049 (mismo criterio click-vs-drag que `ThreeViewport.vue`).
- **"Generar con IA" visible en la toolbar**: navega a la ruta ya existente del ticket 055 (`/projects/:projectId/mobs/:mobId/texture/generate-ai`) -- reutiliza el pipeline real de 054/055 sin reimplementar nada.
- **TDD real**: `GSelect.spec.ts` (9 tests, nuevo), `TextureColorPicker.spec.ts` (7 tests, nuevo), `TextureCanvas.spec.ts` reescrito (39 tests, cubre las 3 zonas, zoom/pan, selectores propios, color personalizado, toast del cuentagotas, guardado de 4 estados, navegación a IA, y TODOS los AC heredados de 047/048/049 sin debilitar ninguno) -- ningún test de `MobEditor.spec.ts`/`EditorHeader.spec.ts` requirió cambios (esos componentes no se tocaron). Frontend completo: 570/570 tests, `vue-tsc -b` y `eslint --max-warnings 0` sin hallazgos.
- **Gap explícito, pendiente de la sesión principal**: revisión visual en vivo con Claude in Chrome contra el Artifact validado -- este worktree aislado no tiene ningún navegador conectado (`list_connected_browsers` vacío, mismo gap ya repetido en 043/047/048/049/050/055).

**Addendum post-milestone -- fix crítico de "Generar con IA" de textura (`referenceImageId` mal derivado)** (`done/059-...`, NO es un ticket del desglose original de Fase 3, nace de un fallo real reportado por el PO en `studio-dev` -- job `2ea1ba73-2ebc-48c7-8f18-fb77e8c44f20`, error `"El modelo no tiene ninguna imagen de referencia subida..."`):
- **Hallazgo real, auditado en el código**: `TextureGenerationSheetPlanner` (053) derivaba el `referenceImageId` de la sheet leyendo `model.referenceImages()` -- un campo de `MobProjectModel` que NINGÚN flujo real de la aplicación llega a poblar jamás (`MobGenerationService.emptyModelFor` lo inicializa en `List.of()` y nunca se vuelve a tocar; solo los tests lo simulaban a mano). La imagen de referencia real vive exclusivamente en la tabla `reference_images` (`ReferenceImageRepository`), ya resuelta por `TextureGenerationService.startGeneration` (`mostRecentReference`) -- ese id simplemente nunca llegaba al planner. Esto rompía el 100% de las generaciones de textura por IA, en cualquier mob, en cualquier ambiente -- no un flake ni un caso límite.
- **Corrección**: `TextureGenerationSheetPlanner.plan(...)` recibe ahora `referenceImageId` como parámetro EXPLÍCITO (rechaza nulo/en blanco con `IllegalArgumentException`) en vez de derivarlo del modelo; `TextureGenerationService` pasa el id ya resuelto en `TextureGenerationJobContext.referenceImageId()`.
- **Cómo sobrevivió el ciclo de QA/Sonar de 054-058**: `TextureGenerationSheetPlannerTest` construía su fixture de `MobProjectModel` con un `ReferenceImage` sintético que ningún flujo real crea -- el test pasaba en verde simulando exactamente la condición que en producción nunca ocurre. Corregido: el fixture ahora deja `referenceImages()` vacío (fiel a la realidad), y el planner se prueba con el id pasado explícitamente como cualquier caller real haría.
- **Verificación real**: `./gradlew clean test` completo, backend: 412/412 tests, 0 failures. Además, verificado EN VIVO contra `studio-dev` con el mismo mob y el mismo job que originó el reporte (`Carcomido_v1`, parte `head`): el pipeline ya avanza correctamente más allá de `mapeando_caras` -- el `IllegalStateException` del planner ya no ocurre.
- **Hallazgo nuevo y separado, destapado por esta misma verificación en vivo**: el job vuelto a correr falló en una etapa posterior (`generando_textura`) con `"OPENAI_API_KEY no está configurada"` -- ninguno de `deploy/docker-compose.{dev,qa,prod}.yml` define esa variable (solo `ANTHROPIC_API_KEY`, usada para análisis de paleta/mapeo de caras, no para la generación de imagen -- el proveedor está fijo en `openai`, `application.properties`). Antes del fix de 059 esto estaba enmascarado porque el pipeline moría antes de llegar ahí. Corregido en los 3 compose files (mismo patrón que `ANTHROPIC_API_KEY`, ver también el addendum de 060 abajo) -- key agregada por el PO directamente en el servidor.

**Addendum post-milestone -- fix crítico: OpenAI rechaza el tamaño de sheet si no es múltiplo de 16** (`done/060-...`, NO es un ticket del desglose original, destapado por la propia verificación en vivo de 059 una vez agregada `OPENAI_API_KEY`):
- **Hallazgo real**: la API real de OpenAI Images rechazó la llamada real con `400 Bad Request: "Invalid size '58x8'. Width and height must both be divisible by 16."` -- `OpenAiImageProvider.generateTextureSheet` mandaba `sheet.sheetWidth()/sheetHeight()` crudos como parámetro `size`, sin redondear. Rompía la llamada real para cualquier sheet cuya dimensión no calzara por casualidad con un múltiplo de 16 (ej. cualquier cara UV pequeña sin "aumentar" -- el caso común).
- **Cómo sobrevivió el ciclo de QA/Sonar de 051-058**: mismo patrón que 059 -- los 6 tests existentes de `OpenAiImageProviderTest` usaban por casualidad dimensiones ya múltiplos de 16 (64x32/128x128/16x16), nunca una dimensión real "fea" como las que produce `ShelfBinPacker` sobre caras UV pequeñas.
- **Corrección**: `OpenAiImageProvider` redondea `width`/`height` hacia ARRIBA al múltiplo de 16 más cercano antes de mandarlos a la API -- 100% interno a esa clase, seguro porque `TextureSheetSlicer` ya recorta cada placement por su `sheetRect` exacto de la imagen decodificada (el margen extra del redondeo queda simplemente descartado, mismo mecanismo ya usado para "bleed"). Ningún otro punto del pipeline cambia.
- **TDD real**: nuevo test con las dimensiones EXACTAS del caso real que falló (58x8 -> 64x16), confirmado que falla contra el código viejo (`git stash` del fix, re-corrida) antes de aplicar la corrección.
- **Verificación real**: `./gradlew clean test` completo, backend: 414/414 tests, 0 failures.

**Addendum post-milestone -- fix crítico: OpenAI rechaza sheets con aspect ratio mayor a 3:1** (`done/061-...`, NO es un ticket del desglose original, tercer hallazgo consecutivo de la misma verificación en vivo -- 059 destapó 060, 060 destapó este):
- **Hallazgo real**: con `58x8` ya redondeado a múltiplo de 16 (`64x16`, ratio 4:1), la API real rechazó la llamada con `400 Bad Request: "Invalid size '64x16'. The maximum supported aspect ratio is 3:1."`.
- **Corrección**: `OpenAiImageProvider.sizeParam` agranda (nunca encoge) el lado más chico, después del redondeo a múltiplo de 16, hasta que el ratio quede dentro de 3:1 -- en ambas orientaciones. Mismo mecanismo de "margen inerte descartado por `TextureSheetSlicer`" que 060. `58x8` termina en `64x32` (no `64x16`), combinando los 3 hallazgos (059+060+061) en el mismo caso real.
- **TDD real**: 3 tests nuevos (horizontal, vertical, límite exacto 3:1 sin alterar), confirmado que fallan contra el código sin este fix antes de aplicarlo.
- **Verificación real**: `./gradlew clean test` completo, backend: 417/417 tests, 0 failures.
- **Patrón repetido, señalado explícitamente**: tercer bug real seguido (059/060/061) del mismo tipo -- solo la verificación EN VIVO contra la API real (no mocks con valores convenientes) los destapó, uno por uno, iterando error por error.

**Addendum post-milestone -- fix crítico: OpenAI rechaza sheets por debajo del "pixel budget" mínimo** (`done/062-...`, NO es un ticket del desglose original, cuarto hallazgo consecutivo de la misma verificación en vivo -- 059→060→061→este):
- **Hallazgo real**: con el ratio ya corregido (`64x32`, dentro de 3:1), la API real rechazó la MISMA llamada con `400 Bad Request: "Invalid size '64x32'. Requested resolution is below the current minimum pixel budget."` -- a diferencia de 059/060/061, ni la documentación pública de OpenAI ni una búsqueda en vivo dieron un número exacto para ese mínimo.
- **Corrección**: `OpenAiImageProvider` aplica un piso conservador de 256px por lado (`MIN_SIDE_PX`), ANTES del clamp de aspect ratio (una cara que sube al piso puede volver a exceder 3:1 si el otro lado ya es grande -- caso límite verificado con `1536x8` -> `1536x512`). Documentado explícitamente como estimación NO confirmada en vivo -- a ajustar si un futuro intento real todavía lo rechaza.
- **TDD real**: 6 casos fallan contra el código sin este fix, confirmado antes de aplicar la corrección.
- **Verificación real**: `./gradlew clean test` completo, backend: 420/420 tests, 0 failures.
- **Cuarto hallazgo consecutivo del mismo tipo**: la cadena completa 059→060→061→062 confirma lo que el propio ticket 051 ya advertía ("a verificar contra la documentación real cuando 054 conecte este proveedor de punta a punta") -- ninguno de los 4 hallazgos era detectable con mocks, solo con verificación en vivo real.

Este archivo se completa a medida que cada ticket del milestone M0 en adelante aterriza código real (ver `pending/`/`in-process/`/`done/` para el estado de cada pieza).

## Referencias

- Documento de definición aprobado: `docs/definiciones/galgoth-studio-mvp.md`
- ADRs de decisiones técnicas puntuales: `docs/adr/`
