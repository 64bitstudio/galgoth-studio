# Componentes — Galgoth Studio

Sin pantallas productivas implementadas todavía. Este archivo se completa conforme cada pantalla aterrice, siempre contrastada contra su mockup correspondiente (ver "Visual Contract" en `docs/definiciones/galgoth-studio-mvp.md`).

## Sistema de diseño base (ticket `002-sistema-diseno-base-visual-contract`)

Implementado en `frontend/src/design-system/`:

- **Tokens** (`tokens/tokens.css`): colores, tipografía (Inter Variable + JetBrains Mono, self-hosted vía `@fontsource`), escala de espaciado, radios, sombras, anillo de foco — valores fieles al master prompt §2.
- **Componentes base** (`components/`): `GButton` (primary/secondary/danger/ghost, hit target ≥40px), `GSidebar` (Inicio/Mis proyectos/Explorar/Plantillas arriba, Configuración/Usuario abajo — corregido contra el mockup real 2026-09-08, ver Addendum de `docs/definiciones/galgoth-studio-mvp.md`; "Nuevo proyecto" es la tarjeta CTA del dashboard, no un ítem de sidebar; sin "Volver"), `GStatusPill` (estado con ícono + color, nunca solo color), `GPanel` (superficie austera, deliberadamente sin sombra por defecto para desalentar anidar cards), `GTabs` (patrón Modelo/Textura/Animación, con Textura/Animación presentes pero deshabilitadas este ciclo).
- **Íconos** (`icons/`): set propio de línea simple (sin librería externa) — home, folder, búsqueda, grilla, engranaje, avatar, fieles a `mockups/01_inicio_mis_proyectos.png`. `IconNewProject` (voxel+"+") queda sin usar por ahora, reservado para la tarjeta CTA del ticket 021.
- **Gap conocido, documentado a propósito:** "Usuario" en `GSidebar` se renderiza como un ítem de nav simple, sin el tratamiento de tarjeta de perfil (avatar + subtítulo) que muestra el mockup — depende de datos reales que no existen todavía en este ticket.
- **Vitrina de desarrollo** (`design-system/Showcase.vue`, ruta `/dev/design-system`): no es una pantalla productiva — existe para contrastar visualmente el sistema contra `mockups/00_all_views.png` antes de construir pantallas reales sobre él.

Pantallas reales (dashboard, editor, wizard, etc.) todavía no existen — llegan en los tickets 021+.

## Viewport Three.js (ticket `008-threejs-viewport-solo-render-dev-harness`)

Implementado en `frontend/src/viewport/`:

- **`ThreeViewport.vue`**: wrapper que adjunta el canvas Three.js compartido (`ThreeViewportService`, singleton) a su contenedor mientras está montado. No crea su propio `WebGLRenderer` — sienta la base para que 016 (viewport interactivo) y 023 (thumbnails) reutilicen el mismo canvas sin agotar contextos WebGL.
- **`ViewportHarness.vue`** (ruta `/dev/viewport-harness`, NO enlazada desde la navegación productiva): abre el sample real Carcomido directamente en el viewport, sin pasar por creación de proyecto/mob (021/022, que no existen todavía). Se retira o queda oculta detrás de un flag una vez M3 esté listo, igual que `/dev/design-system`.
- Renderiza los 24 cuboids del sample (más un marcador esférico por cada uno de los 6 bones, en su pivote mundial compuesto — puede quedar visualmente oculto dentro de la geometría opaca, es cosmético) usando exclusivamente el `CoordinateSystemContract` para toda transformación.
- Cámara orbital (`OrbitControls`), grid de piso, outline de selección y botón de reset de cámara (ticket 016) — ver `frontend/src/viewport/ThreeViewportService.ts`.

## Jerarquía + selección (ticket `017-jerarquia-seleccion-sincronizada`)

Implementado en `frontend/src/editor/`:

- **`HierarchyPanel.vue`** + **`HierarchyBoneNode.vue`** (recursivo, se auto-referencia por nombre de archivo): árbol bone→cuboids→sub-bones, construido por la función pura `hierarchyTree.ts#buildHierarchyTree` (testeable sin montar un componente). Cada cuboid es un nodo clickeable; los bones son solo organizativos este ciclo (no seleccionables).
- **`selectionStore.ts`**: primer store Pinia real del proyecto — `selectedCuboidId` compartido entre el árbol y el viewport (`ThreeViewport.vue`), sincronización bidireccional real (clic en cualquiera de los dos resalta en el otro).
- El dev harness (`/dev/viewport-harness`) ahora muestra el árbol de jerarquía y el viewport lado a lado como demo de la sincronización.

## Herramientas de transformación + Add/Delete/Duplicate (ticket `018-herramientas-transformacion-add-delete-duplicate`)

Implementado en `frontend/src/editor/` y `frontend/src/viewport/`:

- **`geometryOperations.ts`**: espejo TS puro del `GeometryEngine` backend (005) -- `moveCuboid`/`resizeCuboid`/`rotateCuboid`/`setBonePivot`/`setBoneRotation`/`createCuboid`/`createBone`/`removeCuboid`/`duplicateCuboid`, más `computeBoneRemovalImpact`/`removeBoneCascade` (BFS de descendientes) para la advertencia de borrado en cascada. A diferencia del backend (motor de batch atómico con resolución `tempRef`, pensado para lotes generados por IA), esta versión es un set de funciones puras aplicadas una a una desde la UI de confianza -- sin batch ni `tempRef`. Toda creación/redimensión reutiliza `layoutUv` (007) para recalcular la UV, la misma autoridad determinista que el backend.
- **`draftModelStore.ts`** (Pinia): estado en memoria del mob que se edita -- fuente de verdad única que leen `ThreeViewport.vue` y `HierarchyPanel.vue` (ya no reciben el modelo por prop). Envuelve cada operación de `geometryOperations.ts`, captura rechazos (`InvalidGeometryError`, `UvAtlasOverflowError` de 007, etc.) en `lastError` y los loguea con `console.warn` -- ningún rechazo queda invisible. Persistencia real (guardar/autosave) llega en el ticket 020; por ahora solo vive en memoria.
- **Gizmos 3D de arrastre** (`ThreeViewportService.ts` + `ThreeViewport.vue`, vía `TransformControls` de Three.js): Move/Scale/Rotate sobre el cuboid seleccionado, con el patrón "commit al soltar" -- el drag mueve el mesh libremente en cada frame, pero la mutación al `draftModelStore` (y por lo tanto la validación de geometría) solo ocurre en `mouseUp`, calculando el delta final contra una foto tomada en `mouseDown`. Esto evita que `setModel()` (que reconstruye TODOS los meshes en cada mutación) invalide la referencia que `TransformControls` está arrastrando a mitad de un drag. Si la operación resultante es rechazada (ej. escala a 0), el mesh se refresca a la última posición válida en vez de quedar visualmente desincronizado del modelo real.
  - Translate: delta mundial convertido a espacio local del cuboid (sin cadena de rotación de bones todavía -- el mundo y el local coinciden mientras el bone padre no rote).
  - Scale: multiplicativo directo desde `(1,1,1)`, igual semántica que `resizeCuboid` (escala alrededor del centro).
  - Rotate: el modo "local" de `TransformControls` con un solo anillo arrastrado siempre expone un eje unitario limpio (`rotationAxis`) y un ángulo (`rotationAngle`) -- verificado contra el código fuente real, no asumido. Se mapea directo a un delta de un solo eje. El anillo libre (rotación compuesta sin eje dominante) se ignora deliberadamente: el contrato de `rotateCuboid` solo expresa un delta de un eje por vez.
  - **Hallazgo real de tipos**: `@types/three` no declara `rotationAxis`/`rotationAngle` como propiedades de `TransformControls` (solo sus eventos `*-changed`), aunque existen en runtime vía `defineProperty` en el código fuente real -- requiere un cast de tipos documentado en el código, no es una suposición sobre el comportamiento real.
- **Pivote de bone** (`HierarchyBoneNode.vue`): edición vía 3 inputs numéricos (no gizmo 3D -- los bones no son seleccionables en el viewport, ver 017) que llaman `draft.setPivot`.
- **Borrado con advertencia de cascada** (`HierarchyBoneNode.vue`): antes de confirmar, muestra cuántos bones/cuboids hijos se eliminarían (`computeBoneRemovalImpact`) con botones explícitos Confirmar/Cancelar -- sin `confirm()` nativo del navegador.
- **`EditorToolbar.vue`**: botones Move/Scale/Rotate (controlan el modo del gizmo compartido) + Add cuboid/Add bone/Duplicate/Delete, y muestra `draft.lastError` cuando una operación es rechazada.
- **Verificado en vivo** (Claude in Chrome, `/dev/viewport-harness` contra el fixture real Carcomido, 24 cuboids/6 bones): selección + gizmos de Move/Scale/Rotate confirmados visualmente (el cuboid se mueve/escala/rota y el cambio persiste en el modelo tras soltar), Delete y Add bone confirmados, edición de pivote confirmada (el marcador esférico del bone se reubica), advertencia de cascada + Cancelar confirmados. **Hallazgo real (no un bug -- comportamiento correcto)**: el fixture Carcomido ya satura su atlas UV de 64×64 -- tanto "Add cuboid" como "Duplicate" son rechazados en vivo con `UvAtlasOverflowError` (`el atlas actual (64x64) no alcanza... se requieren al menos 64x88/64x114`), y el rechazo se refleja correctamente en `draft.lastError` sin aplicar ningún cambio parcial. Confirma que la integración con AutoUv (007) valida extremo a extremo, no solo en los tests unitarios.

## Command stack de Undo/Redo (ticket `019-command-stack-undo-redo`)

Implementado en `frontend/src/editor/draftModelStore.ts` (mismo store, no uno separado -- ver razonamiento en el comentario de cabecera del archivo):

- Un "Command" no es una clase propia: es simplemente la referencia al `MobProjectModel` inmediatamente anterior a cada edición exitosa, guardada en una pila (`undoStack`). Esto es seguro y barato porque toda función de `geometryOperations.ts` es pura -- nunca muta su modelo de entrada -- así que las referencias históricas nunca se corrompen por una mutación posterior; no hace falta clonar nada.
- `undo()`/`redo()` mueven el modelo actual entre `undoStack`/`redoStack`, reasignando `model` -- nunca crean ni destruyen una `mob_revision` (ese concepto ni siquiera existe todavía en este store, llega en el 020). Una operación RECHAZADA (geometría inválida, referencia inexistente) nunca genera un Command -- no hay nada que deshacer de un no-op.
- `load()` (cargar un mob nuevo) reinicia ambas pilas -- un mob nuevo empieza una historia de edición nueva, no hereda la del anterior.
- Un Command nuevo aplicado después de un `undo()` descarta la rama de redo pendiente (historial lineal estándar, sin árbol de ramas).
- **`EditorToolbar.vue`**: botones Undo/Redo (deshabilitados según `draft.canUndo`/`draft.canRedo`) + atajos de teclado estándar Cmd/Ctrl+Z (deshacer) y Cmd/Ctrl+Shift+Z (rehacer), ignorados mientras el foco esté en un `<input>`/`<textarea>` para no pelear con el undo nativo del propio campo (ej. editando un pivote).
- **Verificado en vivo** (Claude in Chrome, `/dev/viewport-harness`): Delete → Undo (restaura el cuboid) → Redo (lo vuelve a eliminar) confirmado tanto con los botones como con los atajos de teclado (Ctrl+Z / Ctrl+Shift+Z), sin errores de consola.

## Dashboard "Mis proyectos" + CRUD de proyectos (ticket `021-crud-proyectos-dashboard`)

Primera pantalla productiva real del proyecto -- reemplaza `HomePlaceholder.vue`. Implementado en `frontend/src/projects/`:

- **`ProjectsDashboard.vue`** (rutas `/` y `/projects`, mockup 01): saludo + dos tarjetas CTA ("Proyecto vacío", funcional, abre `ProjectNameModal`; "Crear un mob con IA", visible pero deshabilitada con su razón como texto -- sin pipeline de IA todavía) + grid de `ProjectCard`. Simplificación consciente y documentada en la cabecera del propio archivo: el mockup separa "Inicio" (saludo + "Proyectos recientes" + link "Ver todos") de "Mis proyectos" (listado completo) -- el AC de este ticket solo describe la segunda, así que un único dashboard con el listado COMPLETO sirve para ambas entradas del sidebar.
- **`ProjectCard.vue`**: hasta 3 miniaturas (placeholder genérico si `thumbnailKey` es `null` -- siempre, hasta que exista un pipeline de thumbnails) + indicador "+N" cuando el proyecto tiene más de 3 mobs (AC #3) + `GMenu` con Rename/Duplicate/Export(deshabilitado)/Delete. Clic en la tarjeta navega al detalle; clic en el menú de acciones NO dispara esa navegación (`@click.stop`).
- **`GMenu.vue`** (`design-system/components/`, nuevo, primer uso de un menú desplegable en el proyecto): un ítem deshabilitado muestra su razón como texto visible bajo la etiqueta, nunca solo un tooltip -- mismo criterio de accesibilidad ya establecido en `GTabs` (ticket 002).
- **`ProjectNameModal.vue`**: un solo componente para "Nuevo proyecto" (HU-01) y "Rename" -- ambos flujos son idénticos salvo título/valor inicial/verbo del botón. Solo pide nombre (HU-01 AC #3, sin campos técnicos de IA/geometría), rechaza vacío con un mensaje visible antes de llamar al backend (que igual revalida -- defensa en profundidad, mismo criterio que el resto del proyecto).
- **Borrado con confirmación explícita** (en `ProjectsDashboard.vue`, mismo patrón que el borrado en cascada de bones del ticket 018): nombre del proyecto en el mensaje, botones Cancelar/Eliminar explícitos, sin `confirm()` nativo.
- **`projectsApi.ts`**: primer cliente HTTP real del frontend -- `fetch` directo (sin proxy de Vite) contra `VITE_API_BASE_URL` (default `http://localhost:8080`), ya que `docs/definiciones/galgoth-studio-mvp.md` §9 confirma CORS como la estrategia elegida para el origen local de desarrollo, no un proxy. `ApiError` normaliza cualquier respuesta no-2xx (incluida una sin body JSON parseable) a un error consistente `{message, status, code}`.
- **`ProjectDetailPlaceholder.vue`** (ruta `/projects/:id`, ticket 021): deliberadamente mínimo en ese momento -- solo nombre + conteo de mobs, para que la redirección de HU-01 AC #1 tuviera un recurso real. **Reemplazado en el ticket 022** por `ProjectDetail.vue` (ver abajo).
- **Verificado en vivo** (Claude in Chrome, contra el backend real vía `./gradlew bootRun` + Postgres real vía `docker compose`, no solo tests): crear proyecto → redirección real a `/projects/{uuid}` confirmada; Rename, Duplicate (copia profunda verificada contra la API real) y Delete (soft-delete, desaparece del listado) confirmados desde el menú de una tarjeta; indicador "+2" confirmado con 5 mobs sembrados directo en Postgres; navegación del sidebar confirmada; sin errores de consola. **2 hallazgos reales corregidos a partir de esta verificación en vivo** (no detectados por ningún test hasta entonces): fechas serializando como epoch numérico en vez de ISO-8601, y el `@select` del sidebar cableado como no-op pese a que el comentario del código ya afirmaba lo contrario -- ver detalle completo en `docs/ARQUITECTURA.md` y el `## Hecho` del ticket.

## Detalle de proyecto + CRUD de mobs + "Agregar mob" (ticket `022-crud-mobs-detalle-proyecto-agregar-mob`)

Implementado en `frontend/src/projects/` -- reemplaza `ProjectDetailPlaceholder.vue` (ticket 021) con la pantalla real (mockup 12):

- **`ProjectDetail.vue`** (ruta `/projects/:id`): header (nombre + conteo + botón "+ Agregar mob") + buscador (filtro **client-side** por nombre sobre la lista ya cargada, AC #3 -- sin parámetro de búsqueda en el backend, mismo criterio de simplicidad que el dashboard de proyectos) + grid de `MobCard` + tarjeta CTA "+ Nuevo mob" al final del grid (misma acción que el botón del header).
- **`MobCard.vue`**: miniatura (placeholder genérico mientras no exista pipeline de thumbnails, mismo criterio que `ProjectCard.vue`) + nombre + `GStatusPill` de estado (AC #4) -- `mobs.status` en la BD usa guion bajo (`in_progress`), `GStatusPill` usa guion medio (`in-progress`); `MobCard.vue` traduce entre ambas convenciones.
- **`AddMobModal.vue`** (mismo patrón de `<dialog>` nativo que `ProjectNameModal.vue`, ticket 021): nombre + selector del `baseType` real del dominio (5 pills: Humanoide/Arácnido/Cuadrúpedo/Volador/Personalizado, los mismos valores del `CHECK` de `mobs.base_type`). Preserva la idea útil de `add_mob_modal_legacy.png` (selección categorizada antes de confirmar) adaptada a Galgoth: ese legacy modal (de otro producto, "Texture Studio MC") deja elegir un mob ESPECÍFICO de un catálogo de vanilla Minecraft para re-texturizarlo -- algo que no existe en este producto (los mobs de Galgoth se generan/editan libremente, no son reskins), así que la "categoría" real aquí es el `baseType`, no un catálogo de bestiario.
- **`mobsApi.ts`**: mismo patrón que `projectsApi.ts` (fetch directo, reutiliza la clase `ApiError`).
- **Verificado en vivo** (Claude in Chrome, backend real + Postgres real vía `docker compose`): grid con los 5 mobs reales de un proyecto sembrado directo en Postgres, cada uno con su pill "Draft"; buscador filtrando por nombre en tiempo real; "Agregar mob" (Serpentario, tipo Arácnido) creado end-to-end y confirmado directamente contra la base de datos real -- `base_type='arachnid'`, `status='draft'`, `current_revision_number=0`, cero filas en `mob_drafts` (AC #2 verificado contra el dato real, no solo el test); sin errores de consola.

## Pipeline de thumbnails + botón "Guardar" real (ticket `023-pipeline-thumbnails-client-side`)

Implementado en `frontend/src/editor/`, `frontend/src/viewport/` y `frontend/src/api/`:

- **`ThreeViewportService.captureThumbnail()`**: reutiliza el renderer/escena singleton del viewport (008) -- mueve la cámara al ángulo isométrico fijo (`DEFAULT_CAMERA_POSITION`/`TARGET`, ya existentes desde 016), renderiza un frame, captura vía `canvas.toBlob('image/png')`, y en un `finally` SIEMPRE restaura la posición/target de cámara previos (incluso si la captura falla) para que la vista del usuario no salte.
- **`draftPersistenceApi.ts`**: primer cliente frontend de `POST /api/mobs/{mobId}/revisions` -- el ticket 020 solo implementó ese endpoint en el backend; este es el primer consumidor real, invocado desde el botón "Guardar".
- **`thumbnailApi.ts`**: cliente de `POST /api/mobs/{mobId}/thumbnail` (body: bytes crudos del PNG, `Content-Type: image/png`).
- **`EditorToolbar.vue`**: botón "Guardar" real (AC del ticket -- el 020 nunca lo agregó, decisión explícita del Product Owner para no dejar el AC de este ticket sin una forma real de dispararlo). Patrón "commit primero, thumbnail después": `saveRevision()` se ejecuta primero; solo si tiene éxito se intenta `captureThumbnail()`+`uploadThumbnail()` en un try/catch independiente que únicamente hace `console.warn` si falla -- el thumbnail es un asset derivado best-effort, nunca revierte ni bloquea el Guardar ya completado. Muestra el mensaje real del backend (`"Guardado (revisión N)."` o el motivo si no hubo cambios) y cualquier error de `saveRevision` de forma visible.
- **`ProjectCard.vue`/`MobCard.vue`**: actualizados para anteponer `API_BASE_URL` (`thumbnailUrl()`, nuevo helper en `src/api/apiConfig.ts`) al `thumbnailKey` recibido del backend -- ya no es `null` para siempre como en 021/022, ahora es una ruta relativa servible (`/api/mobs/{id}/thumbnail`) que necesita la URL completa del backend para renderizarse como `<img src>`.
- **`ViewportHarness.vue`** (dev harness, 008): al montar, busca-o-crea un proyecto/mob real ("Dev Harness"/"carcomido-harness", vía las APIs de 021/022) y sobreescribe `mobId`/`projectId` del fixture estático de Carcomido con los ids reales -- Guardar necesita un mob que exista de verdad en el backend; la geometría rica del fixture (24 cuboids/6 bones) se conserva intacta.
- **Verificado en vivo** (Claude in Chrome, backend + Postgres + MinIO reales vía `docker compose`): click en "Guardar" desde el harness crea una revisión real (confirmado contra Postgres: `current_revision_number=1`), genera y sube un thumbnail real (PNG 1459×1221 descargado y confirmado visualmente como el ángulo isométrico correcto del mob, no un frame negro/vacío), y `mobs.thumbnail_key` queda con el path servible correcto -- visible de inmediato tanto en el grid de `ProjectDetail.vue` (`MobCard`) como en la miniatura del dashboard (`ProjectCard`). Un segundo Guardar sin cambios de geometría muestra correctamente "Sin cambios..." y aun así reintenta subir el thumbnail (sobreescritura in-place confirmada por el cambio de `updated_at`).

## Wizard de generación IA (ticket `027-wizard-4-pasos-ui`, HU-10, mockups 02-04)

Implementado en `frontend/src/ai/` (ruta `/projects/:projectId/mobs/new-ai`, botón "Crear con IA" en el header de `ProjectDetail.vue`, junto a "+ Agregar mob"):

- **`WizardStepper.vue`**: indicador visual de progreso (4 pasos fijos, no navegable hacia adelante -- el AC exige avanzar linealmente completando cada paso).
- **`AiMobWizard.vue`**: orquesta los 4 pasos. Orden de efectos reales, importante: la imagen elegida en "Referencia" se retiene SOLO en memoria (nunca se sube todavía) -- subirla requiere un `mobId` real (024) y el mob ni siquiera tiene nombre hasta "Configuración". Recién al confirmar ese paso se crea el mob de verdad (022, `POST /api/projects/{id}/mobs`) y DESPUÉS se sube la imagen ya retenida (024, `POST /api/mobs/{mobId}/references`) -- ambas llamadas reales, en ese orden.
- **`steps/ReferenceStep.vue`** (paso 1, REAL): dropzone + input de archivo, valida formato/tamaño client-side con los mismos límites que el backend (PNG/JPEG, 10MB) para feedback inmediato -- el servidor igual revalida (defensa en profundidad, mismo criterio que el resto del proyecto).
- **`steps/ConfigurationStep.vue`** (paso 2, REAL): nombre + tipo base (5 valores manuales, mismo patrón que `AddMobModal.vue`) + resolución de textura (solo UI por ahora, nada en el backend la consume todavía). **Simplificación consciente, VoBo del PO**: el mockup muestra "Detectar con IA" como una opción de tipo base pre-seleccionada por defecto -- se omite este ciclo porque no hay nada real que "detectar" (ticket 028, que la alimentaría, no existe todavía).
- **`steps/GenerationStep.vue`** (paso 3, **REAL desde el ticket `029`**, HU-11; emite `completed`(jobId) desde el `030`): dispara `POST /api/mobs/{mobId}/generate`, abre `GET /api/jobs/{jobId}/events` (SSE nativo del navegador, `EventSource`) y renderiza las 4 etapas reales (no las 6 del mockup -- "Preparando UV"/"Generando textura" no existen este ciclo, ver `GenerationStage.java` del backend) con progreso/mensaje en vivo. El modelo emergente se renderiza en `GenerationPreviewViewport.vue` a medida que llegan eventos `preview_operations`, aplicados vía `generationEvents.ts` (`applyPreviewDelta`, función pura). Botón "Cancelar" con confirmación explícita en dos pasos (mismo patrón que el borrado en cascada de `HierarchyBoneNode.vue`, 018) -- nunca `confirm()` nativo. Al completar, emite `completed` con el `jobId` real -- nunca hace el fetch del resultado ni navega por su cuenta, eso es de `AiMobWizard.vue` (030). El botón "Ir al proyecto" queda solo como escape hatch de fallido/cancelado.
- **`GenerationPreviewViewport.vue`** (`frontend/src/ai/`): envuelve `ThreeViewportService` DIRECTO, sin pasar por `useDraftModelStore` -- a diferencia de `ThreeViewport.vue` (el viewport del editor manual, 016/017/018), el modelo que renderiza es puramente transitorio/descartable (AC #2: nunca `mob_drafts`/`mob_revisions`), de solo lectura (sin selección/gizmos).
- **`steps/ResultStep.vue`** (paso 4, **REAL desde el ticket `030`**, HU-12, mockup 04): componente puramente presentacional (mismo criterio que `ConfigurationStep.vue`) -- recibe todo por props y solo emite intención (`discard`/`regenerate`/`apply`), nunca hace fetch ni navega. Las 3 acciones piden confirmación en dos pasos antes de emitir (mismo patrón que `HierarchyBoneNode.vue`, 018). `jobId` es la única prop opcional (default `null`): sin ella asume que la está montando `/dev/wizard-result-harness` con datos de ejemplo y lo dice explícito en pantalla; con `jobId` real (desde el wizard) no muestra ese aviso. Estado real de compatibilidad FMM (`fmmCompatible`/`fmmIssues`) mostrado tal cual llega, con cada hallazgo real listado (nunca un mensaje genérico, mismo criterio de `FmmCompatibilityValidator`, 013).
- **`AiMobWizard.vue`** orquesta también el paso 4 (030): al recibir `completed` de `GenerationStep`, llama `GET /api/jobs/{jobId}/result` (`generationResultApi.ts`) y muestra `ResultStep` con el resultado real (o un estado de carga/error con reintento si el fetch falla). `discard` navega a `/projects/:id` sin llamar al backend (nada se creó todavía). `regenerate` limpia el resultado y vuelve al paso `'generation'` -- `GenerationStep` se remonta y dispara un `POST /generate` nuevo, reutilizando la misma imagen de referencia ya subida. `apply` llama `POST /api/jobs/{jobId}/apply` y, si tiene éxito, navega a `/projects/:id` también. **Gap conocido, documentado a propósito (VoBo del PO en 030)**: navega al proyecto en vez de abrir un editor real -- ninguna ruta real de "Editar modelo" existe todavía (016-018 solo se ejercieron vía `/dev/viewport-harness`).
- **`frontend/src/api/referenceImagesApi.ts`**: primer cliente frontend de la API de imágenes de referencia (024) -- mismo patrón de body crudo que `thumbnailApi.ts`. **`frontend/src/api/generationApi.ts`** (029): `startGeneration`/`cancelGeneration` (HTTP) + `eventsUrl` (armado de URL, sin `fetch` -- se le pasa directo al `EventSource`). **`frontend/src/api/generationResultApi.ts`** (nuevo, 030): `getGenerationResult`/`applyGeneration`.
- **Verificado en vivo, ticket 027** (Claude in Chrome, backend+Postgres+MinIO reales): subida del archivo real `carcomido_reference.png` (3MB) desde "Referencia" → avanza a "Configuración" con la imagen real mostrada → confirmar crea el mob de verdad (`base_type='arachnid'` reflejando la selección) y sube la referencia de verdad (`width:1254,height:1254` en la BD) → avanza a "Generación" con su aviso honesto → "Ir al proyecto" navega de vuelta y el mob nuevo aparece en el grid real. Sin errores de consola en ningún paso.
- **Verificado en vivo, ticket 029** (Claude in Chrome, backend+Postgres+MinIO reales, API REAL de Anthropic -- `AI_VISION_PROVIDER=claude`/`AI_REASONING_PROVIDER=claude`): click "Generar con IA" con `carcomido_reference.png` real → las 4 etapas avanzaron en vivo, con el mensaje real de silueta detectada por Claude → el rig se construyó incrementalmente en el viewport 3D a medida que llegaban los eventos `preview_operations` (28 operaciones reales) → "Generación completada", `ai_jobs.status='completed'` confirmado en Postgres. Cancelación verificada aparte: click "Cancelar" mid-flight (durante la llamada real al reasoning provider) → confirmación → "La generación fue cancelada por el usuario.", `ai_jobs.status='cancelled'`/`proposal_jsonb IS NULL` confirmado en Postgres. Sin errores de consola.
- **Verificado en vivo, ticket 030** (Claude in Chrome, backend+Postgres+MinIO reales, API REAL de Anthropic): generación real completa contra `carcomido_reference.png` → avance AUTOMÁTICO a "Resultado" (sin ningún clic manual) con conteos reales (`Cuboides: 14, Bones: 7, Textura: 128×128`) y `Compatibilidad FMM: Compatible` (calculado en vivo) → click "Usar este modelo" con confirmación explícita → navega de vuelta a `/projects/:id`, el mob nuevo visible en el grid real. Confirmado en Postgres: `mobs.current_revision_number=1`, `mob_revisions` con `revision_number=1`/`created_by='ai'`, `mob_drafts` con `draft_version=1`. Sin errores de consola.

## Pantallas previstas (12, ver mockups/00_all_views.png del build pack)

1. Inicio / Mis proyectos
2. Nuevo proyecto / mob setup
3. Generación IA
4. Resultado IA (Descartar / Regenerar / Usar este modelo)
5. Editor de modelo (`hierarchy | viewport | inspector`)
6. Edición mediante IA
7. Editor de textura *(fuera de alcance del Technical Alpha)*
8. Generador IA de textura *(fuera de alcance del Technical Alpha)*
9. Animación *(fuera de alcance del Technical Alpha)*
10. Biblioteca de animaciones *(fuera de alcance del Technical Alpha)*
11. Exportación
12. Detalle de proyecto

Estructura de carpetas prevista en `frontend/src/` según `docs/definiciones/galgoth-studio-mvp.md` (Diseño técnico §3).
